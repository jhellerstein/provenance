\documentclass[acmsmall,review,anonymous]{acmart}

\usepackage{microtype}
\raggedbottom
\usepackage{enumitem}
\usepackage{amsthm}
\usepackage{caption}
\usepackage{booktabs}
\usepackage{tikz}
\usepackage{xcolor}

% Tighten paragraph heading spacing
\makeatletter
\renewcommand{\paragraph}{\@startsection{paragraph}{4}{\z@}%
  {0.5ex \@plus 0.2ex \@minus 0.1ex}%
  {-1em}%
  {\normalfont\normalsize\bfseries}}
\makeatother


% ---------- Notation ----------
\newcommand{\Hist}{\mathcal{H}}
\newcommand{\Spec}{\mathsf{Spec}}
\newcommand{\drule}{\mathrel{:\!-}}
\newcommand{\snap}{\mathsf{snap}}
\newcommand{\seal}{\mathsf{seal}}
\newcommand{\FCW}{\mathsf{FCW}}
\newcommand{\seq}{\mathbin{\triangleright}}
\newcommand{\Obs}{\mathsf{Obs}}
\newcommand{\Ord}{\preceq}
\newcommand{\jmh}[1]{\textcolor{red}{\textbf{[JMH:} #1\textbf{]}}}
% ---------- Theorem environments ----------
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}
\theoremstyle{remark}
\newtheorem{remark}{Remark}
\newtheorem{example}{Example}
\newtheorem{observation}{Observation}
\newtheorem{assumption}{Assumption}

\numberwithin{definition}{section}
\numberwithin{theorem}{section}
\numberwithin{lemma}{section}
\numberwithin{corollary}{section}
\numberwithin{remark}{section}
\numberwithin{example}{section}
\numberwithin{observation}{section}
\numberwithin{assumption}{section}

\title{Determination Provenance: From Ambiguity to Algebra}
\author{Joseph M. Hellerstein}
\affiliation{%
  \institution{University of California, Berkeley and Amazon Web Services}
  \country{USA}
}
\date{\today}

\acmConference[Conference]{Full Conference Name}{Month Year}{Location}
\acmBooktitle{Proceedings of the Conference}
\acmYear{2026}
\acmISBN{123-4-56789-012-3}


\begin{document}

\begin{abstract}
  Algebraic provenance traces derivations under a fixed, resolved semantics.
  We extend it to settings with \emph{semantic ambiguity}---where multiple
  consistent outcomes are admissible for the same history---by introducing
  \emph{determination provenance}: a $K$-relation that records, for each
  way of resolving the ambiguity, how a query result is derived under that
  resolution.
  Its supports form a commutative semiring---the \emph{determination
  semiring}---recovering Green et al.'s framework at a new level.
  The layered structure of individual determinations is reflected as a
  \emph{filtration} of sub-semirings that query evaluation respects.

  We instantiate the framework for transactional systems and for Datalog
  with negation.
  For Datalog, the classical negation semantics---stratified,
  well-founded, and stable-model---differ only in which filtration level
  they read; the underlying determination semiring is the same.
  Classical provenance is the degenerate case where no commitments are
  needed.
  In both settings, deciding whether an outcome is robust (holds under
  every resolution) is coNP-complete.
  Determination provenance addresses longstanding gaps in provenance for
  queries with negation and why-not explanations, and enables new
  expressive power for systems settings---answering robustness,
  counterfactual, and tail-bound queries over transaction schedules and
  distributed traces that have no analog in classical provenance.
\end{abstract}

\maketitle

% ================================================================
\section{Introduction}
\label{sec:intro}

Algebraic provenance has given database theory a powerful and durable language for
explaining query results.
When a query has well-defined semantics---a single correct answer for each
input---provenance traces derivations through a commutative
semiring~\cite{green2007provenance,suciu2011probabilistic}.
The algebraic structure provides far more than tracing: compositional
explanation~\cite{green2007provenance},
counterfactual reasoning
(why-not)~\cite{buneman2001why,halpern2001causes}, and quantitative
measures~\cite{suciu2011probabilistic}.
Explanation reduces to arithmetic, and this reduction has been
remarkably effective.

Despite this success, formal data provenance has had limited impact on the
systems that most need explanation: transactional databases, distributed
services, and concurrent pipelines.
These systems record execution traces and lineage
metadata~\cite{sigelman2010dapper,seltzer2005provenance,bates2015trustworthy},
but none of this expressive power---counterfactuals, composition,
quantification---reaches their tracing infrastructure.
The obstacle is a semantic mismatch: these systems operate under
\emph{semantic ambiguity}---a transactional workload may admit multiple
serializable executions; a distributed system may admit multiple orderings
consistent with the same observed history---and classical provenance theory
presupposes the very resolution that these systems leave open.

This mismatch gives rise to provenance questions that have no analog in the
classical setting.
An interbank settlement is rejected because a counterparty's funds
transfer has not yet cleared; classical provenance can explain
\emph{how} the balance was computed, but not \emph{why} the transfer that
would have prevented the rejection arrived too late---that explanation
requires identifying the scheduling commitment that ordered the balance
check before the transfer posted.
Was the rejection inevitable, how likely is it to recur, and would
prioritizing incoming transfers prevent it?
These are questions about the \emph{assumptions} under which the outcome
was produced, not about the derivation itself.
The same phenomenon arises in database theory: a Datalog program with
negation may admit multiple models, and whether a derived tuple holds
may depend on which model is chosen.

This paper extends provenance to settings with semantic ambiguity.
The key idea is simple: before we can explain \emph{how} a result was
derived, we must first account for \emph{which semantic commitments} made
that derivation possible.
We call such a sequence of irrevocable commitments a \emph{determination}.
Once a determination is fixed, classical provenance applies; the
interesting structure lives \emph{within} a determination (commitments
organize into non-commuting layers) and \emph{across} determinations.

Across determinations, the space of resolutions forms a semiring that
lifts the compositional, counterfactual, and quantitative power of
algebraic
provenance~\cite{green2007provenance,buneman2001why,suciu2011probabilistic}
to the ambiguous setting.
The layered structure within each determination is reflected as a
\emph{filtration} of this semiring---a chain of sub-semirings that query
evaluation respects---giving the framework genuine algebraic depth beyond
the support structure alone.
We instantiate the framework for transactional systems (where ambiguity
arises from concurrency) and Datalog with negation (where it arises from
multiple models).
In both, deciding robustness is coNP-complete.
For Datalog, the classical negation semantics---stratified, well-founded,
stable-model---emerge as different readings of one determination semiring.

\begin{example}[Provenance under transactional ambiguity]
  \label{ex:example}
  Consider a relation $S(k,y)$ with initial contents $\{S(0,b)\}$.
  Three transactions execute concurrently with a query:
  \[
    T_1\!: \textsf{ins } S(1,b);\qquad
    T_2\!: \textsf{ins } S(2,d);\qquad
    T_3\!: \textsf{del } S(2,d);\qquad
    T_Q\!: Q(y) \drule S(k,y).
  \]
  All four are concurrent; any serialization is possible.

  Value $b$ appears in $Q$'s result regardless of ordering:
  the initial tuple $S(0,b)$ is never deleted.
  Thus $b$ is \emph{robust}.
  Value $d$ is \emph{contingent}: it appears if $T_2$'s insert takes effect
  before $T_Q$'s read and $T_3$'s delete takes effect after, but is absent
  if $T_3$'s delete precedes $T_Q$'s read.
  Before resolving this ambiguity, even the \emph{form} of the provenance
  question for $d$ is undetermined: in one determination the relevant
  question is \emph{why} $d$ appears, while in another
  it is \emph{why-not} $d$.
  Determination provenance makes this dependence explicit by recording the
  commitments under which the query result is determined, together with the
  derivational explanation that applies once those commitments are fixed.
  Section~\ref{sec:transactions} formalizes this example.
\end{example}

\paragraph{Contributions.}
\begin{enumerate}[label=(\roman*),nosep,leftmargin=*]
  \item The \emph{determination semiring} and its \emph{filtration}:
        the space of resolving determinations forms a commutative semiring
        whose layered structure is reflected as a chain of sub-semirings
        that query evaluation respects
        (Sections~\ref{sec:det-semiring}--\ref{sec:filtration}).
  \item Instantiation for transactional systems: determination depth
        governed by conflict-graph topology
        (Section~\ref{sec:transactions}).
  \item Instantiation for Datalog with negation: the classical negation
        semantics---stratified, well-founded, stable-model---are
        filtration levels of a single determination semiring
        (Section~\ref{sec:negation-preview},
        Appendix~\ref{sec:appendix-datalog}).
  \item Robustness (does a tuple hold under every determination?) is
        coNP-complete in both instantiations
        (Section~\ref{sec:robustness-complexity}).
\end{enumerate}


% ================================================================
\section{Preliminaries: Ambiguous Semantics and Refinement}
\label{sec:prelim}
% ================================================================

Classical data provenance assumes a fixed, resolved semantics: for each input
instance, a query determines a unique result set, and provenance explains how
each tuple in that set was derived.
In many systems, however, concurrency, negation, or underspecification admit
multiple admissible results for the same input.

This section introduces a minimal semantic framework for reasoning about
ambiguity.
We model specifications with multiple admissible outcomes and the
commitments—called \emph{determinations}—that restrict ambiguity until
meaning is determinate.

\subsection{Histories}

\begin{definition}[History]
  A \emph{history} is a finite partially ordered set
  $
    H = (E, \rightarrow),
  $
  where $E$ is a set of events and $\rightarrow \subseteq E \times E$ is a strict
  (irreflexive and transitive) partial order representing known precedence constraints among events.
\end{definition}

\noindent
A history represents a snapshot of what has happened so far in one
particular run of a system: which events have occurred and which
preceded which.
The class $\Hist$ of all histories represents all possible states across
all possible runs.
When the partial order is total, a history is a sequence (e.g., a
transaction schedule).
A database instance for classical provenance would naturally be represented
as a history of unordered tuple insertions: that history ``initializes''
the instance.

Ambiguity arises because histories are incomplete: new events may still
occur, and which events arrive can change which outcomes are admissible.
To model how a history evolves as new events are added, we define an
extension relation.

\begin{definition}[History Extension]
  For histories $H_1=(E_1,\rightarrow_1)$ and $H_2=(E_2,\rightarrow_2)$, we write
  $H_1 \sqsubseteq H_2$ if:
  \begin{enumerate}[nosep,label=(\arabic*)]
    \item $E_1 \subseteq E_2$, and
    \item $\rightarrow_1 = \rightarrow_2 \cap (E_1 \times E_1)$, and
    \item no event in $E_1$ has a predecessor in $E_2 \setminus E_1$.
  \end{enumerate}
\end{definition}

An extension may add new events and precedence edges involving them, but
may not revise the past of any existing event.

\subsection{Outcomes and Specifications}
An \emph{outcome} is an element of a set $O$ of externally
observable results (e.g., a query result, a database state, or a model of a
logic program).
The outcome domain carries a partial order
$\Ord$~\cite{hellerstein2026coordinationcriterion}.
When $o_1 \Ord o_2$, replacing outcome $o_1$ with $o_2$ is not considered
to rule out $o_1$ but rather to \emph{refine} it---$o_2$ adds detail
without contradicting $o_1$.
The order $\Ord$ is part of the specification: it determines which
transitions between outcomes count as refinement (upward in $\Ord$) and
which count as genuine changes (developed in
Section~\ref{sec:three-forces}).

\begin{definition}[Specification]
  A \emph{specification} is a triple $(\Hist, O, \Ord)$ together with a
  relation $\Spec \subseteq \Hist \times O$
  associating each history with a set of \emph{admissible outcomes}.
  We write $\Spec(H) \triangleq \{o \in O \mid (H,o) \in \Spec\}$ for the
  admissible set at~$H$.
\end{definition}

\noindent
A specification is a \emph{relation}: a given input (history) may map to
multiple admissible outcomes, reflecting unresolved semantic ambiguity.
A specification that maps every history to exactly one outcome is a
\emph{function}---no ambiguity remains.
The goal of determination is to narrow a relational specification to a
functional one.

\begin{definition}[Determined specification]
  \label{def:determined}
  A specification is \emph{determined} if it is a function:
  $|\Spec(H)| = 1$ for every $H \in \Hist$.
\end{definition}


\subsection{Commitments}

Ambiguity is constrained by making additional semantic commitments.
We model commitments as constraints on the admissible set that
take effect at specific points in the history.

\begin{definition}[Commitment]
  \label{def:commitment}%
  A \emph{commitment} $\varphi$ is a function on outcome sets:
  given a history $H$ with admissible set $\Spec(H)$, applying $\varphi$
  produces a nonempty subset
  $\Spec(H \cdot \varphi) \subseteq \Spec(H)$.
  We write $H \cdot \varphi$ for the history obtained by
  adding a commitment event $\varphi$ after all maximal
  events of~$H$
  (i.e., $e \to \varphi$ for every $e$ with no successor
  in~$H$), giving $H \sqsubseteq H \cdot \varphi$.
  The result may depend on the full set $\Spec(H)$, not only on
  individual outcomes---this is what allows commitments to be
  non-commutative.
  We require \emph{shrinkage}: $\Spec(H') \subseteq \Spec(H)$ for every
  $H' \sqsupseteq H \cdot \varphi$.
\end{definition}

\noindent
A commitment event succeeds all maximal events of~$H$, inducing a cut in the
history: everything before the commitment is fixed, and the admissible set
after the commitment can only be smaller.
We write \mbox{$(\Spec \mid \varphi)(H) \triangleq \Spec(H \cdot \varphi)$}:
the admissible outcomes after appending $\varphi$ to $H$.

\paragraph{Commitment bases.}
A \emph{commitment basis} $\Phi$ is the vocabulary of constraints available
for narrowing a specification---the set of conditions from which
determinations are formed.
Different bases for the same specification yield different determination
structures and different provenance.
We impose one restriction on bases throughout this paper:

\begin{definition}[Persistent commitment basis]
  \label{def:persistent}
  A commitment basis $\Phi$ is \emph{persistent} if for every
  $\varphi \in \Phi$ and all histories $H_1 \sqsubseteq H_2$:
  if applying $\varphi$ at $H_1$ excludes an outcome $o$, then
  applying $\varphi$ at $H_2$ also excludes $o$.
  Formally, for all outcomes $o \in O$,
  $
    o \notin \Spec(H_1 \cdot \varphi)
    \Longrightarrow
    o \notin \Spec(H_2 \cdot \varphi).
  $
\end{definition}

\noindent
Persistence is distinct from shrinkage.
Shrinkage is a property of a single commitment event applied at a fixed
history: after applying $\varphi$ at $H_1$, the admissible set does not
re-grow at extensions of $H_1 \cdot \varphi$.
Persistence compares two events with the same predicate applied at
different histories: applying $\varphi$ at a later
$H_2 \sqsupseteq H_1$ must exclude at least the same outcomes as applying
it at $H_1$.
(A ``majority vote'' predicate satisfies shrinkage but not persistence:
at $H_1$ with votes $2{:}A, 1{:}B$ it excludes $B$; at $H_2$ with
$2{:}A, 3{:}B$ it excludes $A$ instead.)

Persistence is the paper's main structural assumption.
It guarantees that determined specifications are history-invariant
(Theorem~\ref{thm:resolution-implies-monotone}), enabling classical
provenance once all commitments have been discharged.
The sealing basis for Datalog with negation, the commit/abort basis for
serializability, and the SI basis (snapshot selections and FCW predicates)
are all persistent: applying any of these predicates at a later history
$H_2 \sqsupseteq H_1$ excludes at least the same outcomes as applying it
at $H_1$, because new events can add conflicts or atoms but cannot
rehabilitate an outcome that was already ruled out.

\paragraph{Three forces on a history.}
\label{sec:three-forces}%
A history evolves under three distinct
forces~\cite{hellerstein2026complexity}, organized into two categories.

\emph{Environment events}---input arrivals, message deliveries, operation
interleavings---are outside the system's control.
They extend the history with new information but are not chosen.

\emph{Decision events} are within the system's control.
They come in two kinds, distinguished by whether they rule out futures.
A \emph{commitment} operates on $2^O$: it replaces the current
admissible set $\Spec(H)$ with a proper subset
$\Spec(H \cdot \varphi) \subsetneq \Spec(H)$, irrevocably
excluding some outcomes; under a persistent basis, excluded outcomes
stay excluded.
An \emph{entailment} operates on $O$ via $\Ord$: it replaces
the current observed outcome $o$ with some $o'$ where
$o \Ord o'$---an upward move that the specification recognizes
as adding detail, not contradicting what came before.
When $\Ord$ is flat (no nontrivial refinement), every decision is a
commitment; when $\Ord$ is rich, some decisions are mere entailments.

\begin{example}[The three forces in two settings]
  In transactional systems: operation interleavings are environment;
  commit/abort decisions are commitments; protocol-level derivations
  (e.g., deadlock detection) are entailments.
  In Datalog with negation: EDB facts are environment; sealing and
  choice predicates are commitments; the alternating fixpoint's
  truth-value classifications are entailments---they discover what
  the stratified commitments already determine, excluding nothing.
\end{example}

Multiple commitments may be applied in sequence.
Because a commitment's effect may depend on which outcomes are jointly
present, the order of application can matter:
$\Spec(H \cdot \varphi \cdot \psi)$ and
$\Spec(H \cdot \psi \cdot \varphi)$ may differ.

\subsection{Determinations}

Individual commitments typically express only partial semantic
commitments.
We therefore model semantic resolution using finite compositions of
commitments, applied sequentially.

\begin{definition}[Determination]
  \label{def:determination}%
  A \emph{determination} over a commitment basis $\Phi$ is a finite
  sequence of commitments applied at successive histories:
  $D = \varphi_1(H_0) \cdot \varphi_2(H_1) \cdot \cdots \cdot \varphi_m(H_{m-1})$
  where $\varphi_i \in \Phi$ and $H_i = H_{i-1} \cdot \varphi_i$.
  The result is $(\Spec \mid D)(H_0) \triangleq \Spec(H_m)$.
  The \emph{determination of a history} $D(H)$ is the subsequence of
  commitment events in $H$, listed in the order induced by $\rightarrow$.
\end{definition}

\noindent
Commitment events form a total order within any history (each succeeds all
maximal events of its prefix), so $D(H)$ is always a well-defined sequence.
Because each $\varphi_i$ acts on $\Spec(H_{i-1})$---which depends on all
preceding commitments---the order of application can matter.

\paragraph{Layer-sequencing notation.}
Commitments that commute (can be applied in any order without changing the
result) may be grouped into \emph{layers}.
We use $\seq$ to denote a sequence of such layers:

\begin{definition}[Sequencing of layers (notation)]
  \label{def:layer-seq}
  Let $L_1,\ldots,L_k$ be multisets of commitments.
  We write $L_1 \seq L_2 \seq \cdots \seq L_k$
  for the determination obtained by applying the commitments of $L_1$ (in any
  order), then those of $L_2$, and so on:
  \[
    (\Spec \mid (L_1 \seq \cdots \seq L_k))(H)
    \;\triangleq\;
    \Spec(H \cdot L_1 \cdot L_2 \cdots L_k),
  \]
  where $H \cdot L$ abbreviates appending all commitments in $L$ after $H$ in any
  order.
  This notation is used only to expose layering; it imposes no additional algebraic
  structure beyond commutativity within each layer.
\end{definition}

\noindent
The \emph{depth} of a determination $L_1 \seq \cdots \seq L_k$ is $k$
(the number of layers).

\begin{definition}[Resolving determination]
  A determination $D$ \emph{resolves} $\Spec$ if $\Spec \mid D$ is determined
  (Definition~\ref{def:determined}): a function from histories to outcomes.
\end{definition}

\subsection{Resolution Enables Provenance}
\label{sec:mono-theorem}

Classical semiring provenance annotates base tuples and propagates
annotations through query evaluation~\cite{green2007provenance}.
This requires a fixed instance---a unique outcome at each input.
When $|\Spec(H)| > 1$, no single annotation can account for all
admissible outcomes simultaneously.

\begin{assumption}[Prefix-closure of histories]
  \label{ass:prefix-closed}
  If $H \in \Hist$ and $H' \sqsubseteq H$, then $H' \in \Hist$.
\end{assumption}

\begin{theorem}[Resolution is necessary and sufficient for provenance]
  \label{thm:resolution-implies-monotone}
  \label{thm:necessity}
  Assume prefix-closure and a persistent commitment basis $\Phi$.
  Classical semiring provenance over $\Spec$---a single $K$-relation
  assigning each tuple a provenance value consistent with all admissible
  outcomes---is well-defined if and only if $\Spec$ is determined.
\end{theorem}

\begin{proof}
  \emph{Necessity.}
  Semiring provenance expresses only positive dependence on base
  facts~\cite{green2007provenance}.
  When two admissible outcomes differ on a fact $f$ (present in $o_1$,
  absent in $o_2$), the ambiguity lies outside the semiring's expressive
  power: no annotation can simultaneously explain both.

  \emph{Sufficiency.}
  A determined specification yields exactly one outcome per history---a
  fixed instance.
  Semiring provenance annotates its base tuples and propagates through
  query evaluation as in~\cite{green2007provenance}.
\end{proof}

\noindent
Under a persistent basis, resolved specifications enjoy a stronger
property that we use freely in subsequent sections:

\begin{lemma}[History-invariance]
  \label{lem:history-invariance}
  Let $\Phi$ be a persistent basis and $D$ a determination that resolves
  $\Spec$.
  Then $(\Spec \mid D)(H_1) = (\Spec \mid D)(H_2)$ for all
  $H_1 \sqsubseteq H_2$.
\end{lemma}

\begin{proof}
  Write $D = \varphi_1 \cdot \cdots \cdot \varphi_m$.
  Since $D$ resolves $\Spec$, $(\Spec \mid D)(H_1) = \{o_1\}$.
  For every $o \neq o_1$, persistence gives
  $o \notin \Spec(H_2 \cdot \varphi_1 \cdots \varphi_m)$
  (applied inductively at each layer).
  Hence $(\Spec \mid D)(H_2) = \{o_1\}$.
\end{proof}

\noindent
History-invariance means the resolved outcome does not depend on which
history prefix we start from---only on the determination applied.
We may therefore write $(\Spec \mid D)$ without a history argument.
Note that this is a retrospective guarantee: it holds over prefixes of a
\emph{completed} history (all environment events have occurred).
New environment events would extend the history and potentially introduce
ambiguity that $D$ does not cover, requiring additional commitments---analogous
to how new messages disturb convergence in an eventually-consistent system.

\subsection{Three Levels of Structure}
\label{sec:three-levels}

The framework operates at three levels:
\begin{enumerate}[nosep,label=(\arabic*),leftmargin=*]
  \item \emph{Within a determination}: a resolving determination has
        internal structure---layers of commitments that discharge
        sequentially until classical provenance applies.
  \item \emph{Across determinations (within a spec)}: the space of
        resolving determinations forms a semiring; robustness,
        contingency, and the coNP complexity of robustness live here.
  \item \emph{Across specs}: preservation asks whether an outcome
        survives a change of specification (e.g., SI to SER, stable
        to well-founded).
\end{enumerate}
Sections~\ref{sec:conditional-semiring}--\ref{sec:robustness-complexity}
develop levels (2) and (3); level (1) is detailed in
Appendix~\ref{app:algebra}.

%% ================================================================
\section{The Algebra of Determinations}
\label{sec:conditional-semiring}

Classical provenance owes its power to algebraic structure: derivations
compose through a commutative semiring, and query evaluation preserves
that structure~\cite{green2007provenance}.
One might expect that layered determinations---sequences of non-commuting
commitments---resist such clean algebraic treatment.
In fact, the space of all resolving determinations forms a commutative
semiring, and the layering within each determination is reflected as a
filtration of sub-semirings that query evaluation respects.

Throughout, we fix a specification $\Spec$ over a history class $\Hist$ and
a persistent commitment basis $\Phi$; the set of minimal resolving
determinations is denoted $\mathcal{D}_{\Spec,\Phi}$ (abbreviated
$\mathcal{D}$ when $\Spec$ and $\Phi$ are clear from context).

\subsection{The Determination Semiring}
\label{sec:det-semiring}
\label{sec:algebra}

Fix a commutative semiring $(K, +, \cdot, 0, 1)$ for derivational provenance
(e.g., the polynomial semiring $\mathbb{N}[X]$ of~\cite{green2007provenance}).
A \emph{$K$-relation} assigns each tuple a value in $K$: alternative
derivations combine by $+$, joint use of facts by $\cdot$, absence by $0$.
For a fixed determination $D \in \mathcal{D}$, the \emph{conditioned
provenance} $P_D(t) \in K$ is the classical semiring provenance of tuple $t$
computed over the determined specification $\Spec \mid D$
(well-defined by Theorem~\ref{thm:resolution-implies-monotone}).

Conditioned provenance suffices for explaining a single execution.
But some questions require reasoning \emph{across} determinations:
Does this result hold under every admissible commit ordering, or is it
contingent on a particular one?
Answering such questions requires comparing provenance across multiple
determinations.

The resulting structure is a Boolean algebra on supports.
Let $\mathcal{D}$ denote the set of minimal resolving determinations for
$\Spec$ over a commitment basis $\Phi$: those resolving determinations
with no proper subsequence that also resolves $\Spec$.
This set is finite (finitely many commitments, each applied at most once).

\begin{definition}[Determination Provenance]
  \label{def:det-provenance}
  For a specification $\Spec$, provenance query $P$, and tuple $t$, the
  \emph{determination provenance} of $t$ is the function
  \[
    P_{\Spec}(t) : \mathcal{D} \to K
  \]
  mapping each resolving determination $D \in \mathcal{D}$ to the conditioned
  provenance $P_{\Spec \mid D}(t)$, with value $0$ when
  $t \notin (\Spec \mid D)$.
  When $\Spec$ is clear from context, we write $P(t)$.
\end{definition}

\noindent
Determination provenance is a $K$-relation over
determinations: it assigns each $D \in \mathcal{D}$ a semiring value
recording how $t$ is derived under $D$.
Conditioned provenance is the special case of evaluating at a single point:
$P(t)(D) = P_D(t)$.
Classical provenance is the special case where $|\mathcal{D}| = 1$
(the specification is already determined; no commitments needed).

The \emph{support}
$\mathrm{supp}(P(t))
  \triangleq
  \{\, D \in \mathcal{D} \mid P(t)(D) \neq 0 \,\}$
is the set of determinations under which $t$ holds.

These supports form a commutative semiring under the operations induced by
query evaluation:
joint use of facts (semiring multiplication) maps to \emph{intersection}
of supports ($t$ holds only under determinations supporting both premises),
while alternative derivations (semiring addition) map to \emph{union}
($t$ holds under any determination supporting either path).
The resulting structure on supports is the Boolean algebra
$(2^{\mathcal{D}}, \cup, \cap, \emptyset, \mathcal{D})$---a commutative
semiring with $\cup$ as addition and $\cap$ as multiplication.
This is the \emph{determination semiring}: it captures which determinations
support a tuple and how those supports compose through queries.
(The determination semiring operates on \emph{supports}---subsets of
$\mathcal{D}$; the richer object \emph{determination provenance}
(Definition~\ref{def:det-provenance}) pairs each determination with a
$K$-valued derivational annotation.
The two structures are independent: supports compose in the determination
semiring; annotations compose in the provenance semiring $K$.)
The Boolean algebra on supports is elementary; the non-trivial structure
is the \emph{filtration} (Section~\ref{sec:filtration}), which reflects
the layered commitment process and distinguishes determination provenance
from mere set-membership tracking.

An outcome is \emph{robust} if its support is all of $\mathcal{D}$
(it holds under every determination).
An outcome is \emph{robustly absent} if its support is empty
(it fails under every determination).
An outcome with nonempty proper support is \emph{contingent}:
it holds under some determinations but not others.
An outcome is \emph{preserved} from specification $\Spec$ to $\Spec'$ if
whenever it holds under some determination of $\Spec$, it also holds under
some determination of $\Spec'$:
$\forall D \in \mathcal{D},\;
(o \in (\Spec \mid D)) \Rightarrow
\exists D' \in \mathcal{D}'.\; (o \in (\Spec' \mid D'))$.
Robustness asks whether an outcome survives all resolutions of a fixed
specification; preservation asks whether it survives a change of
specification---e.g., from snapshot isolation to serializability
(Section~\ref{sec:transactions}).
Vandevoort et al.~\cite{vandevoort2025mixed} study exactly this question
for transaction programs: which transactions can run at a weaker isolation
level while guaranteeing serializable behavior?
The same question arises for Datalog negation: does a derived tuple survive
a change from stable to well-founded semantics
(Appendix~\ref{sec:appendix-datalog})?
Determination provenance provides a uniform language for such analyses
across domains.

\begin{remark}[Symmetry of presence and absence]
  \label{rem:absence-symmetry}
  The determination semiring treats presence and absence symmetrically:
  a contingent tuple has partial support (present under some
  determinations, absent under the complement).
  Classical why-not provenance explains absence derivationally under a
  fixed semantics; determination provenance adds the semantic dimension,
  recording which commitments made the absence (or presence) possible.
\end{remark}

\subsection{Notation and Example}

In Green et al.'s framework, provenance is read off as a semiring
expression: $P(t) = x_1 \cdot x_2 + x_3$ means ``derived from $x_1$ and
$x_2$ together, or from $x_3$ alone.''
Determination provenance is a function from determinations to provenance
values, displayed as a set of pairs (omitting zero entries):
\[
  P(t) = \{\,(D,\; p) \mid D \in \mathcal{D},\; p = P_D(t) \neq 0\,\}.
\]
Entries with zero provenance (tuple absent under that determination) are
omitted.
The support $\mathrm{supp}(P(t))$ is the projection onto the first
component.
The two algebraic structures are independent: within each pair, the
provenance value lives in the classical provenance semiring $K$; across pairs, the
supports compose in the determination semiring
$(2^{\mathcal{D}}, \cup, \cap)$.

\begin{example}[Determination provenance for Example~\ref{ex:example}]
  \label{ex:det-semiring}
  With $\mathcal{D} = \{D_{\mathsf{in}}, D_{\mathsf{out}}\}$:
  \[
    P(b) = \{(D_{\mathsf{in}},\, x_0),\; (D_{\mathsf{out}},\, x_0)\}
         = \mathcal{D} \times \{x_0\},
    \qquad
    P(d) = \{(D_{\mathsf{in}},\, x_2)\}.
  \]
  Here $x_0$ annotates the initial tuple $S(0,b)$ and $x_2$ annotates
  $T_2$'s insert of $S(2,d)$.
  $P(b)$ has full support ($\mathcal{D}$): $b$ is robust.
  $P(d)$ has partial support ($\{D_{\mathsf{in}}\}$): $d$ is contingent.

  Composition operates on both components.
  A join multiplies provenance values pointwise (within each determination)
  and intersects supports:
  $P(b \bowtie T(b)) = \{(D_{\mathsf{in}},\, x_0 \cdot y),\;
  (D_{\mathsf{out}},\, x_0 \cdot y)\}$---still robust.
  A union adds provenance values pointwise and unions supports:
  $P(b) \cup P(d) = \{(D_{\mathsf{in}},\, x_0 + x_2),\;
  (D_{\mathsf{out}},\, x_0)\}$---nonzero under every determination,
  since $b$ is present under both.
\end{example}

\paragraph{Computational cost.}
$|\mathcal{D}|$ may be exponential in the size of the commitment basis.
When all commitments commute (a single-layer determination), supports admit compact
representation as positive Boolean formulas over commitment
variables (Proposition~\ref{prop:collapse-depth-1}).
For deeper determinations, compact representation requires respecting inter-layer
validity constraints; this remains open.

\subsection{Filtration of the Determination Semiring}
\label{sec:filtration}

In Example~\ref{ex:example}, the provenance of $b$ is the same under both
$D_{\mathsf{in}}$ and $D_{\mathsf{out}}$---knowing which determination was
applied adds nothing to the explanation.
The provenance of $d$ differs across determinations: explaining $d$ requires
knowing that $D_{\mathsf{in}}$ was applied.
This distinction is not accidental.
The layered structure of a determination---forced by non-commutativity of
commitments---is reflected as genuine algebraic structure in the semiring:
a \emph{filtration} (a nested chain of sub-semirings
$\mathcal{F}_0 \subseteq \mathcal{F}_1 \subseteq \cdots$) that query
evaluation respects.
The layers are not merely an operational artifact of sequential commitment;
they are reflected as algebraic strata in the semiring that compose cleanly
with relational algebra.
Throughout this subsection, we fix the layering of each
determination: commitments are grouped into maximal commuting batches
given the semantic commutativity induced by the determination's prefix
(analogous to Foata normal form in trace
monoids~\cite{mazurkiewicz1977concurrent}).
Since commutativity may depend on the prefix, different determinations
may have different layerings; the filtration is well-defined because
``agreement at level $k$'' compares only determinations sharing the same
prefix through layer $k$.
Different valid orderings within a layer do not affect the construction.

Two determinations $D, D' \in \mathcal{D}$ \emph{agree at level $k$} if
they apply the same commitments in layers $1, \ldots, k$.
That is, writing $D = L_1 \seq L_2 \seq \cdots \seq L_m$ and
$D' = L'_1 \seq L'_2 \seq \cdots \seq L'_{m'}$,
agreement at level $k$ means $L_i = L'_i$ for $i = 1, \ldots, k$.
Agreement at level $k$ is an equivalence relation on $\mathcal{D}$;
write $[D]_k$ for the equivalence class of $D$ at level $k$.
These classes refine as $k$ increases:
$[D]_0 = \mathcal{D}$ (vacuous) and $[D]_k = \{D\}$ when $k$ equals the
number of layers (full agreement).

\begin{definition}[Level-$k$ support]
  A set $S \subseteq \mathcal{D}$ is a \emph{level-$k$ support} if it is a
  union of level-$k$ equivalence classes.
  Equivalently, $S$ is closed under level-$k$ agreement:
  if $D \in S$ and $D'$ agrees with $D$ at level $k$, then $D' \in S$.
\end{definition}

\begin{definition}[Filtration]
  Define $\mathcal{F}_k \triangleq \{S \subseteq \mathcal{D} \mid
  S \text{ is a level-}k\text{ support}\}$.
  This yields:
  \[
    \mathcal{F}_0 \;\subseteq\; \mathcal{F}_1 \;\subseteq\; \cdots
    \;\subseteq\; \mathcal{F}_d \;=\; 2^{\mathcal{D}},
  \]
  where $\mathcal{F}_0 = \{\emptyset, \mathcal{D}\}$.
\end{definition}

\noindent
$\mathcal{F}_0$ captures the coarsest distinction: $\mathcal{D}$ is the
support of a robust outcome (holds under every determination) and
$\emptyset$ is the support of an impossible one (holds under none).
No layer information is needed to make this distinction.

\begin{proposition}[Filtration respects the semiring]
  \label{prop:filtration-semiring}
  Each $\mathcal{F}_k$ is closed under $\cup$ and $\cap$, hence
  $(\mathcal{F}_k, \cup, \cap, \emptyset, \mathcal{D})$ is a
  sub-semiring of the determination semiring for each $k$.
\end{proposition}
\begin{proof}
  Unions and intersections of unions-of-equivalence-classes are again
  unions-of-equivalence-classes.
\end{proof}

\begin{proposition}[Single-layer case]
  \label{prop:collapse-depth-1}
  When all commitments commute (a single layer), the filtration has exactly
  two levels
  ($\mathcal{F}_0 = \{\emptyset, \mathcal{D}\}$ and
  $\mathcal{F}_1 = 2^{\mathcal{D}}$), and the determination semiring
  coincides with $\mathrm{PosBool}(\Phi)$---positive Boolean formulas
  over commitment variables, with $\vee$ as addition and $\wedge$ as
  multiplication.
\end{proposition}
\begin{proof}
  With a single layer, two determinations agree at level $1$ iff they
  apply the same set of commitments---that is, iff they are the same
  determination.
  The level-$1$ equivalence classes are therefore singletons $\{D\}$.
  A level-$1$ support is any union of such singletons (by definition),
  which is an arbitrary subset of $\mathcal{D}$; hence
  $\mathcal{F}_1 = 2^{\mathcal{D}}$.
  Each determination corresponds to a conjunction of commitment variables
  (which commitments were applied); supports correspond to disjunctions
  of such conjunctions---exactly $\mathrm{PosBool}(\Phi)$.
\end{proof}

\begin{definition}[Query-relative depth]
  \label{def:query-depth}
  The \emph{query-relative depth} of a tuple $t$ is
  $\mathrm{qdepth}(t) \triangleq
  \min\{k \mid \mathrm{supp}(P(t)) \in \mathcal{F}_k\}$.
\end{definition}

\begin{proposition}[Characterization]
  \label{prop:qdepth-characterization}
  \begin{enumerate}[nosep,label=(\alph*)]
    \item $\mathrm{qdepth}(t) = 0$ iff $t$ is robust
          ($\mathrm{supp} = \mathcal{D}$) or impossible
          ($\mathrm{supp} = \emptyset$).
    \item $\mathrm{qdepth}(t) = k$ iff all determinations agreeing on
          layers $1, \ldots, k$ agree on whether $t$ holds, but some
          pair agreeing on layers $1, \ldots, k{-}1$ disagrees.
    \item $\mathrm{qdepth}(t) = d$ iff $t$'s presence depends on the
          full determination.
  \end{enumerate}
\end{proposition}
\begin{proof}
  (a)~$\mathcal{F}_0 = \{\emptyset, \mathcal{D}\}$ by definition.
  (b)~$\mathrm{supp} \in \mathcal{F}_k$ means the support is closed under
  level-$k$ agreement, so layers $k{+}1, \ldots, d$ are irrelevant.
  Minimality of $k$ means some level-$(k{-}1)$ class is split.
  (c)~Immediate from (b).
\end{proof}

\begin{example}[Query-relative depth in the transactional setting]
  \label{ex:qdepth-txn}
  In Example~\ref{ex:example}, the determination has a single layer (all commit
  decisions commute when no cycles exist).
  Value $b$ has $\mathrm{qdepth}(b) = 0$: it holds under every
  determination (robust).
  Value $d$ has $\mathrm{qdepth}(d) = 1$: its support is a proper subset
  of $\mathcal{D}$ (the determinations where $T_Q$ commits before $T_3$),
  which is not in $\mathcal{F}_0 = \{\emptyset, \mathcal{D}\}$.
  Classical provenance suffices for $b$; determination provenance is needed
  for $d$.
\end{example}

\begin{corollary}[Query evaluation respects the filtration]
  \label{cor:propagation-filtration}
  Under positive relational algebra:
  $\mathrm{qdepth}(t_1 \bowtie t_2) \le
  \max(\mathrm{qdepth}(t_1), \mathrm{qdepth}(t_2))$,
  and likewise for union.
  Query evaluation cannot increase query-relative depth beyond the maximum
  depth of its inputs.
\end{corollary}
\begin{proof}
  Join computes $\mathrm{supp}(t_1) \cap \mathrm{supp}(t_2)$;
  union computes $\mathrm{supp}(t_1) \cup \mathrm{supp}(t_2)$.
  By Proposition~\ref{prop:filtration-semiring}, both remain in
  $\mathcal{F}_{\max(k_1, k_2)}$.
\end{proof}

\begin{proposition}[Difference can increase depth]
  \label{prop:difference-increases-depth}
  Under relational difference, query-relative depth can strictly increase:
  there exist tuples $t_1, t_2$ with $\mathrm{qdepth}(t_1) = 0$ and
  $\mathrm{qdepth}(t_2) > 0$ such that
  $\mathrm{qdepth}(t_1 \setminus t_2) > \mathrm{qdepth}(t_1)$.
\end{proposition}
\begin{proof}
  Let $t_1$ be robust ($\mathrm{supp} = \mathcal{D}$, depth~$0$) and
  $t_2$ contingent ($\mathrm{supp} = S \subsetneq \mathcal{D}$,
  depth~$> 0$).
  Then $t_1 \setminus t_2$ holds under exactly the determinations in
  $\mathcal{D} \setminus S$---a proper nonempty subset of $\mathcal{D}$
  (assuming $S \neq \emptyset$ and $S \neq \mathcal{D}$).
  Hence $\mathrm{supp}(t_1 \setminus t_2) = \mathcal{D} \setminus S
  \notin \mathcal{F}_0$, so $\mathrm{qdepth}(t_1 \setminus t_2) > 0$.
\end{proof}

\noindent
Corollary~\ref{cor:propagation-filtration} and
Proposition~\ref{prop:difference-increases-depth} together show that the
filtration is tight: positive relational algebra preserves depth, while
negation (difference) can increase it.
Moreover, every filtration level is realized:

\begin{proposition}[Reachability]
  \label{prop:reachability}
  For a determination of depth $d$ with non-trivial choices at each
  layer, every level $0 \le k \le d$ is the query-relative depth of
  some tuple.
\end{proposition}
\begin{proof}
  Level~$0$: any base tuple present in all determinations (robust).
  Level~$k$ for $1 \le k \le d$: a tuple whose presence depends on the
  choice at layer~$k$ but not on layers $k{+}1, \ldots, d$ has support
  that is a union of level-$k$ classes but not of level-$(k{-}1)$
  classes.
\end{proof}

\noindent
The sequential structure that non-commutativity imposes is not a deficiency
but a reflection of algebraic richness:
Section~\ref{sec:negation-preview} gives a striking application where the
classical negation semantics for Datalog correspond to different levels of
the same filtration.


% ================================================================
\section{Transactions as Semantic Ambiguity}
\label{sec:transactions}
% ================================================================

We now instantiate the framework in transactional systems, where ambiguity
arises from concurrency: multiple consistent executions may be admissible for
the same observed history.
We formalize the commitment basis, characterize number of layers across
isolation levels, and show how determination provenance distinguishes robust
from contingent query results.

\subsection{Transactional Histories}

\begin{definition}[Transactional history]
  \label{def:txn-history}
  A \emph{transactional history} is a history $H = (E, \rightarrow)$ whose
  events include:
  \begin{itemize}[nosep]
    \item $\mathsf{begin}(T_i)$, $\mathsf{commit}(T_i)$, $\mathsf{abort}(T_i)$
          --- lifecycle events for transaction $T_i$;
    \item $\mathsf{r}(T_i, x, v)$, $\mathsf{w}(T_i, x, v)$ --- read and write
          operations by $T_i$ on object $x$ with value $v$.
  \end{itemize}
  Commit and abort events are the commitment events of this setting;
  all other events (transaction arrivals, reads, writes) are environment-driven
  additions to the history.
  (Scheduling decisions can also serve as commitments; see
  Section~\ref{sec:scheduling-basis}.)
  Aborted transactions remain visible in the history: an abort is a
  commitment that resolved a conflict.
\end{definition}

\begin{definition}[Conflict]
  \label{def:conflict}
  Two operations from distinct transactions $T_i \neq T_j$ \emph{conflict} if
  they access the same object $x$ and at least one is a write.
  Following Adya~\cite{adya1999weak}, we distinguish three conflict types:
  \emph{write-write} ($\mathsf{ww}$: both write $x$),
  \emph{write-read} ($\mathsf{wr}$: $T_i$ writes $x$, $T_j$ reads $x$), and
  \emph{read-write} ($\mathsf{rw}$: $T_i$ reads $x$, $T_j$ writes $x$;
  also called an \emph{anti-dependency}).
\end{definition}

\begin{definition}[Conflict graph]
  \label{def:conflict-graph}
  The \emph{conflict graph} $G(H)$ of a transactional history $H$ is the
  directed graph whose vertices are the \emph{active} (uncommitted,
  non-aborted) transactions in $H$ and whose edges represent conflicts:
  $T_i \to T_j$ if $T_i$ and $T_j$ conflict and $T_i$'s conflicting
  operation precedes $T_j$'s in $H$.
  When $T_i$ commits, all edges adjacent to $T_i$ are removed from the
  active conflict graph: $T_i$'s position in the serial order is finalized.
  When $T_i$ aborts, all edges adjacent to $T_i$ are likewise removed
  (its operations are rolled back).
\end{definition}

\subsection{Isolation Levels as Specifications}

Following Adya~\cite{adya1999weak}, each isolation level is characterized by
which cycles in the conflict graph are forbidden.
The specification $\Spec_L$ maps a history to the set of outcomes
(committed database states) consistent with $L$'s acyclicity constraint:
stronger levels forbid more edge types in cycles.
We consider three levels:
\emph{read committed} (RC; no constraint on committed conflict graphs),
\emph{serializability} (SER; forbids all cycles in the full conflict graph),
and \emph{snapshot isolation} (SI; multiversion with first-committer-wins
on write-write conflicts).

\subsection{Commitment Basis}

The interleaving of read and write operations is determined by the
environment: operations arrive non-deterministically and are serviced
immediately.
The conflict edges that result are therefore \emph{environment events}---they
are observed, not chosen.
The system's only irrevocable decisions are \emph{commit} and \emph{abort}:

\begin{definition}[Commit commitment]
  \label{def:commit-commitment}
  The commitment $\varphi_{\mathsf{commit}(T_i)}$ irrevocably finalizes
  $T_i$'s operations: $T_i$'s effects become durable and $T_i$ is removed
  from the active conflict graph (along with all its adjacent edges).
  This commitment is valid only if, after removing $T_i$, the acyclicity
  invariant of the isolation level is not violated---that is, committing
  $T_i$ does not leave a forbidden cycle among the remaining active
  transactions.
\end{definition}

\begin{definition}[Abort commitment]
  \label{def:abort-commitment}
  The commitment $\varphi_{\mathsf{abort}(T_i)}$ irrevocably discards
  $T_i$'s operations: $T_i$'s effects are rolled back and $T_i$ is removed
  from the active conflict graph.
  Abort is always valid (a transaction may always be aborted).
\end{definition}

\noindent
The commitment basis is thus $\Phi = \{\varphi_{\mathsf{commit}(T_i)},
\varphi_{\mathsf{abort}(T_i)} \mid T_i \text{ active}\}$.
Ordering predicates are not commitments---they are environment facts
recorded in the history as conflict edges accumulate.

\begin{proposition}[Commutativity of commit/abort on independent transactions]
  \label{prop:commit-commute}
  Commit and abort commitments on transactions that share no conflict edges
  commute: their joint effect on the active conflict graph and the admissible
  set is independent of application order.
\end{proposition}
\begin{proof}
  If $T_i$ and $T_j$ share no edges, removing one does not affect the
  other's adjacency or the validity of committing/aborting the other.
  The resulting conflict graph is the same regardless of order.
\end{proof}

\begin{remark}[Non-commutativity under shared conflicts]
  \label{rem:commit-noncommute}
  When $T_i$ and $T_j$ share a common cycle, committing one may
  make committing the other invalid (the remaining cycle would violate the
  acyclicity invariant), while aborting one may make committing the other
  valid (the cycle is broken).
  In such cases, commit/abort decisions do not commute, and the
  determination has depth $> 1$.
\end{remark}

\subsection{Determination Depth by Isolation Level}

\begin{proposition}[Depth by isolation level]
  \label{prop:rc-depth-zero}
  \label{prop:ser-depth}
  \label{prop:si-depth}
  Under read committed, the specification is already determined
  (depth~$0$).
  Under serializability with the commit/abort basis: depth~$1$ when the
  conflict graph is acyclic or has only disjoint cycles; depth~$\ge 2$
  when cycles overlap.
  Under snapshot isolation, the basis adds snapshot predicates
  $\varphi_{\snap(T_i) = U}$ and first-committer-wins predicates
  $\varphi_{\mathrm{fcw}(x, T_i)}$; worst-case depth is $O(n)$ from
  chains of FCW-to-snapshot dependencies.
\end{proposition}

\begin{example}[Overlapping cycles and depth]
  \label{ex:overlapping-cycles}
  Consider five transactions with two conflict cycles sharing $T_3$:
  $T_1 \to T_2 \to T_3 \to T_1$ (Cycle~1) and
  $T_3 \to T_4 \to T_5 \to T_3$ (Cycle~2).
  Aborting $T_1$ breaks Cycle~1 but leaves Cycle~2 intact, requiring a
  second abort whose validity depends on the first---so the two decisions
  do not commute and depth is~$\ge 2$.
  Aborting $T_3$ instead breaks both cycles in one decision (depth~$1$).
  The determination records which resolution was chosen.
\end{example}

\subsection{Running Example Revisited}

Returning to Example~\ref{ex:example}: $T_2$, $T_3$, and $T_Q$ conflict on
$(2,d)$ with no cycles.
Two representative determinations:
$D_{\mathsf{in}} \triangleq
  \varphi_{\mathsf{commit}(T_2)} \cdot \varphi_{\mathsf{commit}(T_Q)}
  \cdot \varphi_{\mathsf{commit}(T_3)}$
and
$D_{\mathsf{out}} \triangleq
  \varphi_{\mathsf{commit}(T_2)} \cdot \varphi_{\mathsf{commit}(T_3)}
  \cdot \varphi_{\mathsf{commit}(T_Q)}$.
The determination provenance is:
\[
  P(b) = \mathcal{D} \times \{x_0\}, \qquad
  P(d) = \{(D_{\mathsf{in}},\, x_2)\}.
\]
Prior to resolution, even the \emph{form} of the provenance question for $d$
is ambiguous (why vs.\ why-not).
$b$ is robust (nonzero under every determination); $d$ is contingent.
Figure~\ref{fig:det-tables} summarizes the determination structure across
isolation levels for this example.

\paragraph{Expressive power beyond classical provenance.}
The determination semiring enables queries that classical provenance
cannot express.
\emph{Robustness}: is $b$'s presence inevitable, or could a different
serialization have excluded it?
(Full support confirms inevitability.)
\emph{Counterfactuals}: under which commit orderings does $d$ appear?
(The support $\{D_{\mathsf{in}}\}$ answers directly.)
\emph{Tail bounds}: under what fraction of admissible serializations
does a latency-sensitive query meet its SLA?
(The support ratio gives a structural bound without empirical sampling.)
\emph{Diagnosis}: when the SLA is violated, which ordering commitment
is shared by all violating determinations?
(The complement of the support identifies the root cause.)
These questions concern the \emph{space} of admissible executions, not
any single execution---a capability absent from systems that record only
one trace.

\subsection{Enriching the Basis: Scheduling as Commitment}
\label{sec:scheduling-basis}

The commit/abort basis treats operation interleaving as environment.
When a system actively \emph{schedules} conflicting operations, those
ordering decisions are commitments:

\begin{definition}[Ordering commitment]
  \label{def:ordering-commitment}
  $\varphi_{T_i \prec T_j}$ records that the system chose to execute
  $T_i$'s conflicting operation before $T_j$'s, excluding outcomes with
  the reverse order.
\end{definition}

\noindent
The enriched basis is
$\Phi' = \Phi \cup \{\varphi_{T_i \prec T_j} \mid T_i, T_j \text{ conflict}\}$.
Ordering commitments on distinct pairs commute.
The boundary between environment and commitment is a modeling choice
reflecting system architecture; the enriched basis is used in the
robustness reduction of Section~\ref{sec:robustness-complexity}.



\begin{figure*}[t]
  \centering
  \footnotesize
  \setlength{\tabcolsep}{4pt}
  \renewcommand{\arraystretch}{1.3}

  \begin{tabular}{@{}lll@{}}
    \toprule
    \textbf{Isolation / Case} & \textbf{Resolving determination} & \textbf{Depth} \\
    \midrule
    Read committed &
    (none---no outcome contradicted) & $0$ \\
    %
    SER, no cycles &
    $\{\varphi_{\mathsf{c}}(T_1), \varphi_{\mathsf{c}}(T_2), \varphi_{\mathsf{c}}(T_3), \varphi_{\mathsf{c}}(T_Q)\}$ & $1$ \\
    %
    SER, $d$ visible &
    $\varphi_{\mathsf{c}}(T_2) \cdot \varphi_{\mathsf{c}}(T_Q) \cdot \varphi_{\mathsf{c}}(T_3)$ & $1$ \\
    %
    SER, $d$ absent &
    $\varphi_{\mathsf{c}}(T_2) \cdot \varphi_{\mathsf{c}}(T_3) \cdot \varphi_{\mathsf{c}}(T_Q)$ & $1$ \\
    %
    SER, overlapping cycles &
    $\varphi_{\mathsf{a}}(T_1) \seq \varphi_{\mathsf{a}}(T_4)$
    \;\emph{(Ex.~\ref{ex:overlapping-cycles})} & $\ge 2$ \\
    %
    Snapshot isolation &
    $\{\varphi_{\snap(T_6){=}\emptyset},\, \varphi_{\snap(T_7){=}\emptyset}\}
      \seq \varphi_{\mathrm{fcw}(S(5),T_6)}
      \seq \varphi_{\snap(T_Q){=}\{..\}}$ & $O(n)$ \\
    \bottomrule
  \end{tabular}

  \medskip
  {\footnotesize
  $\varphi_{\mathsf{c}}(T) = \varphi_{\mathsf{commit}(T)}$;\quad
  $\varphi_{\mathsf{a}}(T) = \varphi_{\mathsf{abort}(T)}$;\quad
  $\{\cdots\}$ = commuting set (layer);\quad
  $\seq$ = non-commuting sequence (adds a layer).}

  \caption{
    Determination structure across isolation levels (Example~\ref{ex:example}).
    Read committed requires no commitments.
    Serializability has depth~$1$ without cycles, $\ge 2$ with overlapping cycles.
    Snapshot isolation adds depth from FCW-to-snapshot dependencies.
    See Appendix~\ref{sec:appendix-datalog} for Datalog negation.
  }
  \Description{Table showing resolving determinations and their depth for
    read committed (depth 0), serializability (depth 1 or 2), and snapshot
    isolation (depth O(n)).}
  \label{fig:det-tables}
\end{figure*}

% ================================================================
\section{Beyond Transactions: Negation and the Filtration}
\label{sec:negation-preview}
% ================================================================

Datalog with negation provides a second instantiation that demonstrates
two capabilities beyond the transactional setting.
First, the filtration recovers the classical negation semantics---stratified,
well-founded, and stable---as prefixes of a single determination.
Second, determination provenance explains why-not under ambiguity: an atom
can be absent not because of a blocked derivation but because of a semantic
commitment.
(Full details in Appendix~\ref{sec:appendix-datalog}.)

\paragraph{Setup.}
Consider the program
\begin{align*}
  p(c) & \leftarrow a(c).      & r(c) & \leftarrow \neg s(c). \\
  q(c) & \leftarrow \neg p(c). & s(c) & \leftarrow \neg r(c).
\end{align*}
with EDB $\{a(c)\}$.
The pair $(p,q)$ is stratified; the pair $(r,s)$ forms a cycle through
negation.

\paragraph{The specification.}
To model all common negation semantics in one spec, we use an enriched
outcome domain.
An \emph{outcome} assigns each atom a value in
$\{\mathbf{t}, \mathbf{f}, \mathbf{u}, \bot\}$, where $\bot$ means
``not yet evaluated'' and $\mathbf{u}$ means ``genuinely ambiguous.''
The outcome order is
$\bot \Ord \mathbf{u} \Ord \{\mathbf{t}, \mathbf{f}\}$
($\mathbf{t}$ and $\mathbf{f}$ incomparable), applied pointwise.
The \emph{commitment basis} has \emph{sealing predicates}
$\varphi_{\seal}(S_i)$ (stratum $S_i$ is complete---excludes outcomes
where additional atoms in $S_i$ are true) and \emph{choice predicates}
$\varphi_{a=v}$ ($\neg$-cycle atom $a$ has value $v \in \{\mathbf{t},
\mathbf{f}\}$---excludes the alternative).
Both are commitments: they operate on $2^O$, excluding outcomes.
The specification $\Spec_P$ admits all assignments consistent with the
program rules and EDB~\cite{gelfond1988stable}.
After all commitments are discharged, two fully resolved outcomes remain:
$o_r = \{a(c), p(c), r(c)\}$ and $o_s = \{a(c), p(c), s(c)\}$.

The resolving determinations are $\mathcal{D} = \{D^{(r)}, D^{(s)}\}$:
\[
  D^{(r)} = \varphi_{\seal}(S_1) \seq \varphi_{\seal}(S_2) \seq
    \varphi_{r(c)=\mathbf{t}} \cdot \varphi_{s(c)=\mathbf{f}},
  \qquad
  D^{(s)} = \varphi_{\seal}(S_1) \seq \varphi_{\seal}(S_2) \seq
    \varphi_{s(c)=\mathbf{t}} \cdot \varphi_{r(c)=\mathbf{f}},
\]
sharing the stratified prefix (layers~1--2) and branching at layer~3.
The filtration is
$\mathcal{F}_0 = \mathcal{F}_1 = \mathcal{F}_2 = \{\emptyset, \mathcal{D}\}$
and $\mathcal{F}_3 = 2^{\mathcal{D}}$.

\paragraph{Determination provenance.}
\[
  \begin{array}{r@{\;=\;}l@{\qquad}r@{\;=\;}l}
    P(p(c)) & \mathcal{D} \times \{x_a\} \;\text{(robust)} &
    P(r(c)) & \{(D^{(r)},\, 1)\} \\
    P(q(c)) & \{\} \;\text{(robustly absent)} &
    P(s(c)) & \{(D^{(s)},\, 1)\}
  \end{array}
\]
Atoms $r(c)$ and $s(c)$ are contingent: each holds under exactly one
determination.
Under $D^{(r)}$, $s(c)$ is absent because of the commitment
$\varphi_{r(c)=\mathbf{t}}$ itself---not a blocked derivation but a
semantic choice.

\paragraph{The filtration recovers three semantics.}

\begin{theorem}[Negation semantics as filtration levels]
  \label{thm:negation-filtration}
  For a finite normal Datalog program with $k$ stratification layers and
  $\neg$-cycles resolved by choice predicates, the determination semiring
  has depth $k{+}1$.
  The classical negation semantics correspond to filtration levels:
  stable-model semantics reads the full filtration ($\mathcal{F}_{k+1}$);
  well-founded semantics reads $\mathcal{F}_k$ (the sealing prefix) and
  classifies residual ambiguity as $\mathbf{u}$;
  stratified semantics reads $\mathcal{F}_k$ and is defined only when
  the sealing prefix resolves all atoms.
\end{theorem}
\begin{proof}[Proof sketch]
  The sealing prefix $\varphi_{\seal}(S_1) \seq \cdots \seq
  \varphi_{\seal}(S_k)$ is shared by all resolving determinations
  (it is the unique stratified evaluation).
  Hence all determinations agree at level $k$:
  $\mathcal{F}_k = \{\emptyset, \mathcal{D}\}$.
  The choice layer (layer $k{+}1$) distinguishes stable models:
  $\mathcal{F}_{k+1} = 2^{\mathcal{D}}$.
  WFS computes the same sealing prefix and classifies atoms by their
  support at level $k$: robust ($\mathbf{t}$), robustly absent
  ($\mathbf{f}$), or contingent ($\mathbf{u}$)---without discharging
  layer $k{+}1$.
  Full proof in Appendix~\ref{sec:appendix-datalog}.
\end{proof}

\noindent
Under the enriched outcome domain, the correspondence is summarized as:
\begin{center}
\small
\begin{tabular}{@{}lll@{}}
  \toprule
  \textbf{Semantics} & \textbf{Determination prefix} & \textbf{$\neg$-cycle atoms} \\
  \midrule
  Stable & full ($k{+}1$ layers) & $\mathbf{t}$/$\mathbf{f}$ \\
  Well-founded & sealing only ($k$ layers) & $\mathbf{u}$ \\
  Stratified & sealing only ($k$ layers) & $\bot$ \\
  Monotone & none & $\bot$ \\
  \bottomrule
\end{tabular}
\end{center}
Stable semantics discharges all commitments; WFS discharges the sealing
layers and classifies the residual ambiguity as $\mathbf{u}$ (via the
alternating fixpoint---an entailment, not a further commitment;
see Appendix~\ref{sec:appendix-datalog});
stratified semantics discharges the same sealing layers but is defined
only when they resolve everything ($\neg$-cycle atoms remain $\bot$).
The filtration of the determination semiring makes this prefix structure
explicit and compositional: supports at each filtration level compose
through queries (Corollary~\ref{cor:propagation-filtration}), so
preservation across semantics can be checked algebraically rather than
by re-running the alternating fixpoint.

\paragraph{Preservation.}
Preservation across semantics follows directly:
$p(c)$ is preserved from stable to well-founded to stratified (support in
$\mathcal{F}_0$, visible at every level);
$r(c)$ is not (support in $\mathcal{F}_{k+1} \setminus \mathcal{F}_k$,
invisible until the cycle layer is discharged).

% ================================================================
\section{Complexity of Robustness}
\label{sec:robustness-complexity}
% ================================================================

Robustness---whether a tuple holds under \emph{every} resolving
determination---is a natural question in the framework.
Supports compose cleanly through queries
(Corollary~\ref{cor:propagation-filtration}), but deciding whether a
support equals $\mathcal{D}$ is computationally hard.
The hardness is unsurprising in isolation---exponentially many
determinations make universality expensive---but its value is as a
validation: the same abstract theorem yields hardness in both
instantiations via a uniform mechanism, confirming that width is the
right complexity parameter.

\begin{definition}[Determination width]
  \label{def:width}
  The \emph{width} of a layer is the number of independent commitments
  it contains.
  The \emph{width} of a determination is the maximum width of any layer.
  A layer of width $n$ with each commitment selecting among $k_i \ge 2$
  alternatives admits $\prod_{i=1}^n k_i$ resolving determinations
  (exponential in $n$ when all $k_i \ge 2$).
\end{definition}

\begin{theorem}[Robustness is coNP-complete in width]
  \label{thm:robustness-conp}
  For any instantiation where the widest layer has $n$ independent
  binary commitments, validity checking is polynomial, and query
  evaluation is polynomial, deciding robustness is coNP-complete.
\end{theorem}

\begin{proof}
  \emph{Upper bound.}
  To refute robustness, guess a determination $D$ ($n$ binary choices,
  polynomial-size), verify validity, evaluate the query, and check that
  $t$ is absent (polynomial-time certificate).

  \emph{Lower bound.}
  $n$ independent binary commitments give $|\mathcal{D}| = 2^n$,
  bijecting with truth assignments to $n$ variables.
  A UCQ encodes any DNF $\psi$: the distinguished tuple is present under
  $D$ iff the corresponding assignment satisfies $\psi$.
  Robustness reduces to \textsc{DNF-Validity} (coNP-complete).
\end{proof}

\noindent
Both instantiations admit unbounded width:
in the transactional setting (enriched scheduling basis), $n$ pairs of
conflicting transactions on distinct objects give $n$ independent binary
ordering commitments;
in Datalog (stable models), $n$ disjoint $\neg$-cycles of length~2 give
$n$ independent binary choice commitments.
In the Datalog case, the same reduction independently proves
coNP-hardness of skeptical reasoning under stable
models~\cite{marek1991autoepistemic}, validating that the determination
semiring's width-based abstraction captures the right source of hardness.
Full proofs in Appendix~\ref{app:robustness-proofs}.

\begin{corollary}[Tractability at bounded width]
  \label{cor:fpt-width}
  For a single-layer determination of width $w$ (all commitments commute),
  robustness is decidable in time $O(2^w \cdot p(n))$, where $p(n)$ is the
  cost of validity checking and query evaluation---fixed-parameter
  tractable in $w$.
  More generally, for depth $d$ and width $w$, the brute-force bound is
  $O(2^{wd} \cdot p(n))$.
\end{corollary}

\noindent
The coNP-completeness result (Theorem~\ref{thm:robustness-conp}) and
Corollary~\ref{cor:fpt-width} together show that width is the right
complexity parameter: robustness is tractable when width is bounded and
intractable when it grows with the input.
Computing the full support $\mathrm{supp}(P(t))$ (not just checking
whether it equals $\mathcal{D}$) has the same complexity profile:
enumerate determinations and evaluate the query under each.

% ================================================================
\section{Related Work}
\label{sec:related}
% ================================================================

\noindent\textbf{Algebraic provenance.}
Semiring provenance~\cite{green2007provenance} and its extensions to
probabilistic~\cite{suciu2011probabilistic},
factorized~\cite{olteanu2015factorized}, and
aggregate~\cite{senellart2018provenance} settings all operate at
depth~$0$: a fixed, resolved semantics.
Why-not provenance~\cite{buneman2001why}, causal
responsibility~\cite{meliou2010causality}, and counterfactual
explanations~\cite{halpern2001causes} likewise condition on a single
outcome.
Determination provenance generalizes these by making the
commitments that precede derivational provenance explicit and composable.

\medskip
\noindent\textbf{Provenance beyond the monotone fragment.}
Extending semiring provenance to negation is a recognized open problem.
Dannert et al.~\cite{dannert2021semiring} extend semiring provenance to
least-fixed-point logic, handling stratified negation within a single
model.
For stratifiable programs, our within-determination provenance coincides
with theirs once the layers of sealing commitments are discharged.
Our contribution addresses programs with unstratified negation, where
multiple stable models coexist and the determination semiring tracks
which models support each tuple.
Deutch et al.~\cite{deutch2014circuits} use Boolean circuits to represent
provenance for non-monotone queries but likewise assume a single resolved
model.
Köhler et al.~\cite{kohler2012declarative} study why-not provenance for
Datalog with negation under a fixed well-founded model.
All three approaches assume a resolved semantics; our framework is
complementary, explaining which commitments select the model under which
their techniques then apply.

\medskip
\noindent\textbf{Provenance in systems settings.}
Database provenance systems (Perm~\cite{glavic2008perm},
GProM~\cite{arab2018gpromprovenance},
Smoke~\cite{psallidas2018smoke}) trace derivations under a fixed
execution or isolation level.
Vandevoort et al.~\cite{vandevoort2025mixed} study which transactions can
run at weaker isolation while preserving serializable behavior---a
preservation question in our sense.
Our framework is complementary: it separates the semantic commitments
(which execution was chosen) from the derivational explanation (how the
result was produced under that execution), enabling these systems'
per-execution provenance to be composed across alternative resolutions.
Appendix~\ref{app:systems-lineage} develops the connection to systems
lineage in detail.

\medskip
\noindent\textbf{Determination complexity.}
Hellerstein~\cite{hellerstein2026complexity} develops a complexity
theory for determinations, characterizing the cost and depth of semantic
resolution.
The present work focuses on explanation rather than complexity:
determinations are assumed given, and determination provenance explains how
observations depend on them.

\medskip
\noindent\textbf{Consistent query answering.}
Arenas et al.~\cite{arenas1999consistent} introduce consistent query
answering: a tuple is a \emph{certain answer} if it holds in every
repair of an inconsistent database.
The structural analogy to robustness is direct---both universally
quantify over a space of admissible completions---but the source of
multiplicity differs: repairs resolve constraint violations in the data,
while determinations resolve semantic ambiguity in the specification.
Determination provenance additionally tracks \emph{which} completions
support a tuple (the support set), not merely whether all do.
Moreover, repairs are typically defined by minimal symmetric difference,
yielding a flat (single-layer) structure with no inter-commitment
dependencies.
The layered, non-commutative structure that persistence enables---and
the filtration it induces---has no direct analog in the CQA setting.

% ================================================================
\section{Conclusion}
\label{sec:conclusion}
% ================================================================

This paper reframes provenance around a simple observation: many modern systems do
not produce a single semantic outcome from a history, but a space of admissible
ones.
In such settings, explaining an observation requires accounting for how ambiguity
was resolved—not merely how derivations proceeded after the fact.
Determination provenance makes those semantic commitments explicit and
composable, recovering classical algebraic provenance once resolution is fixed
while retaining visibility into the assumptions that made resolution possible.
The expressive power of algebraic provenance---compositional explanation,
counterfactual reasoning, quantitative measures---is thereby lifted from
the resolved setting to the ambiguous one.

The algebraic development shows that determinations are not ad hoc:
the space of resolving determinations forms a semiring whose filtration
reflects the layered structure of individual determinations, and query
evaluation respects this structure compositionally.
As evidence that the determination semiring captures the right level of
abstraction, it recovers several known results as special cases:
classical semiring provenance at depth~$0$;
$\mathrm{PosBool}(\Phi)$ for single-layer determinations;
the three classical negation semantics as filtration levels of a single
object;
and the coNP-completeness of skeptical reasoning under stable models as
an instance of the general width-based robustness theorem.
In transactional systems, determination provenance explains how query
observations depend on commit ordering and isolation commitments.
In logic programming with negation, it exposes preservation across
semantics.

\paragraph{Open directions.}
Developing quantitative robustness measures beyond the binary
robust/contingent distinction; extending the framework to reversible
commitments (without persistence, history-invariance fails and the
determination semiring is no longer a static object---a natural
candidate is \emph{eventual persistence}, where exclusions stabilize
after a finite prefix, but characterizing the weakest sufficient
conditions is open); and characterizing compact
representations of the determination semiring for deep determinations
all remain open.

The success of algebraic provenance lay in identifying the right semantic object
to explain.
Determination provenance extends that insight: when meaning itself depends on
resolution, provenance must explain the assumptions that made meaning possible.

\pagebreak
\appendix

% ================================================================
% ================================================================
\section{Robustness Proofs}
\label{app:robustness-proofs}
% ================================================================

\subsection{Transactional Hardness (Full Proof)}

We reduce from \textsc{DNF-Validity}: given a DNF formula
$\psi = t_1 \vee \cdots \vee t_\ell$ over variables $x_1, \ldots, x_n$,
is $\psi$ true under every truth assignment?
This problem is coNP-complete.

\paragraph{Construction.}
Using the enriched scheduling basis (Section~\ref{sec:scheduling-basis}):
for each variable $x_i$, create two transactions $T_i^+$ and $T_i^-$,
each writing to a distinct object $o_i$.
$T_i^+$ writes $\mathsf{true}$; $T_i^-$ writes $\mathsf{false}$.
Both are concurrent; their write-write conflict on $o_i$ creates an edge.
The ordering commitment $\varphi_{T_i^+ \prec T_i^-}$ or
$\varphi_{T_i^- \prec T_i^+}$ determines the final value of $o_i$
(last-writer-wins).
Since each pair conflicts only on its own object, no cycles arise and
every combination of orderings is a valid resolving determination.
The set $\mathcal{D}$ has $2^n$ elements, one per truth assignment.

\paragraph{Query.}
For each term $t_j = \ell_1 \wedge \cdots \wedge \ell_m$ of $\psi$,
define a conjunctive query $Q_j$ that selects a distinguished tuple
$t^*$ iff the objects corresponding to the literals of $t_j$ have the
matching values.
The overall query is $Q = Q_1 \cup \cdots \cup Q_\ell$ (a union of
conjunctive queries).

\paragraph{Correctness.}
Under any resolving determination $D$, the ordering of each pair
$(T_i^+, T_i^-)$ induces a truth assignment $\alpha_D$ to $x_i$.
The tuple $t^*$ is present in $Q$'s result under $D$ iff $\alpha_D$
satisfies at least one term of $\psi$---that is, iff
$\alpha_D \models \psi$.
Hence $t^*$ is robust (present under every $D \in \mathcal{D}$) iff
$\psi$ is valid (true under every assignment).

\paragraph{Polynomial conditions.}
Condition~(i): each determination is a sequence of $n$ ordering
commitments (polynomial-size).
Condition~(ii): checking validity amounts to verifying that the conflict
graph under the chosen ordering is acyclic (trivially true here, since
no cycles exist).
Condition~(iii): evaluating a UCQ over a polynomial-size database is in
polynomial time.

\subsection{Datalog Hardness}

The general theorem (Theorem~\ref{thm:robustness-conp}) applies directly
to the Datalog instantiation, providing an independent proof of
coNP-hardness via the DNF-Validity reduction.

\paragraph{Instantiation.}
Given $n$ disjoint $\neg$-cycles of length~2, the choice layer has $n$
independent binary commitments (one per cycle).
The DNF-Validity gadget of the transactional proof applies verbatim:
encode a DNF $\psi$ as a UCQ over the choice-induced truth values, so
that the distinguished tuple is robust iff $\psi$ is valid.
This yields coNP-hardness for the Datalog instantiation.

The result coincides with the known coNP-completeness of skeptical
reasoning under stable-model
semantics~\cite{marek1991autoepistemic}---the robustness question
``is $\mathrm{supp}(P(a)) = \mathcal{D}$?'' is exactly the skeptical
reasoning question ``does $a$ hold in every stable model?''
The match validates that the determination semiring's width-based
abstraction captures the right source of hardness.

\paragraph{Polynomial conditions.}
Condition~(i): a determination is a sequence of sealing and choice
commitments, one per stratum plus one per $\neg$-cycle atom
(polynomial-size).
Condition~(ii): checking that a determination produces a valid stable
model requires computing the Gelfond--Lifschitz reduct and verifying
that the proposed model is its minimal model (polynomial time).
Condition~(iii): evaluating a Datalog query over a fixed model is in
polynomial time.

% ================================================================
\section{Within a Determination: Algebraic Details}
\label{app:algebra}
% ================================================================

This appendix gives precise structural content to the layered structure
within a single determination, as introduced in Section~\ref{sec:algebra}.
For the algebraic structure \emph{across} determinations (the
determination semiring and its filtration), see
Appendix~\ref{app:det-semiring-details}.

\subsection{Sequential Discharge Versus Algebraic Combination}

Commitments across layers compose by sequential discharge.
Given a specification $\Spec$ and commitments $\varphi_1,\varphi_2$,
\[
  \Spec(H \cdot \varphi_1 \cdot \varphi_2)
\]
need not equal
\[
  \Spec(H \cdot \varphi_2 \cdot \varphi_1),
\]
because commitments may be ambiguity-sensitive.
Accordingly, the inter-layer sequencing operator $\seq$ is not an algebraic
operation: it is the mechanism that transitions from one monoid to
the next by discharging the current layer and conditioning the specification
for the layer below.

This distinction is essential.
Commitments \emph{restrict} the space of admissible outcomes; they do not
\emph{combine} explanations.
Algebraic combination of explanations occurs only at the semiring foundation,
after all commitment layers have been discharged.

\subsection{Commutative Monoids Within a Layer}

Within a single layer, commitments commute by construction.
Fix a specification prefix $\Spec^{\ell}$ obtained after discharging all
layers above layer~$\ell$.
Let $L$ be the set of commitments in layer~$\ell$.

For $\varphi,\psi \in L$,
\[
  \Spec^{\ell}(H \cdot \varphi \cdot \psi)
  \;=\;
  \Spec^{\ell}(H \cdot \psi \cdot \varphi).
\]
Thus multisets of commitments drawn from $L$ form a commutative monoid
under composition, with the empty multiset (no restriction) as identity.

This monoid is \emph{conditional}: its carrier---which commitments are valid
and what effect they have---depends on the determination prefix.
No global algebra over commitments is assumed or required.
Discharging the monoid (applying all commitments in $L$) produces the
specification prefix for the next layer.

\subsection{The Semiring Foundation}

The key algebraic transition occurs when the last commitment layer is
discharged.

\begin{proposition}[Determined specifications admit semiring provenance]
  \label{prop:semiring}
  If $\Spec \mid D$ is determined and the commitment basis is persistent
  (Definition~\ref{def:persistent}), then derivational provenance over
  $\Spec \mid D$ is captured by a commutative semiring.
\end{proposition}
\begin{proof}[Proof sketch]
  By Theorem~\ref{thm:resolution-implies-monotone}, $\Spec \mid D$ is
  history-invariant.
  Because it is also single-valued (determined), $(\Spec \mid D)(H)$ is a singleton
  for every history $H$; no further commitments can eliminate outcomes, so semantic
  conditioning has ceased.
  History-invariance gives constancy: the unique outcome is the same
  regardless of which history prefix we start from.
  This yields a fixed instance over which derivations compose exactly as in
  monotone query evaluation: alternative derivations of the same fact
  combine by semiring addition, and joint use of multiple facts combines by
  semiring multiplication.
  This is precisely the algebraic regime established
  by Green et al.~\cite{green2007provenance} for positive relational queries.
\end{proof}

\subsection{The Layered Determination as a Whole}

A determination of depth~$k$ over a provenance semiring $K$ consists of
commutative monoids $M_1, M_2, \ldots, M_k$---where each $M_i$ is
conditioned on the specification prefix obtained by discharging
$M_1, \ldots, M_{i-1}$---followed by a commutative semiring $K$ at the
foundation, exposed once all monoids have been discharged.
Layers are numbered bottom-up: $M_1$ is the first layer discharged, $M_k$
the last.
Classical provenance is the degenerate case $k = 0$: no commitment layers,
just the semiring.

Determination provenance recovers the algebraic conditions required for
semiring provenance by explicitly discharging semantic ambiguity through the
layered determination.
Seen this way, a layered determination is a semantic prelude to algebraic
provenance: it explains \emph{when} and \emph{why} classical provenance
applies, rather than assuming a fixed monotone semantics from the outset.

\subsection{Open Questions}
\label{app:algebra-future}

Several directions for algebraic development of the within-determination
structure remain open.

\paragraph{Collapse at depth 1.}
When all commitments commute (a single layer), the determination reduces to a
single commutative monoid tensored with the provenance semiring, and the
determination semiring coincides with
$\mathrm{PosBool}(\Phi)$~\cite{green2007provenance}.

\paragraph{Universality.}
Green et al.\ showed that $\mathbb{N}[X]$ is the most general provenance
semiring for positive relational algebra.
An analogous question: is there a ``most general'' layered determination
from which all others are homomorphic images?
The conditioning between layers (each monoid depends on the discharge of the
previous) makes this non-trivial.

\paragraph{Composition laws.}
When two determinations share a common prefix, they share initial layers.
A formal treatment of determination morphisms---how determinations relate
under prefix extension, restriction, and substitution---would enable
compositional reasoning across sub-specifications.

% ================================================================
\section{Across Determinations: The Determination Semiring}
\label{app:det-semiring-details}
% ================================================================

This appendix expands on the determination semiring and filtration
introduced in Section~\ref{sec:det-semiring}.
Where the body states results compactly, this appendix provides the
full arguments.

\subsection{Supports Form a Semiring}

\begin{proposition}
  The supports $\mathrm{supp}(P(t)) \subseteq \mathcal{D}$ compose
  through positive relational algebra as follows
  (assuming $K$ is zero-divisor-free, as holds for $\mathbb{N}[X]$
  and all standard provenance semirings):
  \begin{enumerate}[nosep,label=(\alph*)]
    \item \emph{Join:}
          $\mathrm{supp}(P(t_1 \bowtie t_2))
          = \mathrm{supp}(P(t_1)) \cap \mathrm{supp}(P(t_2))$.
    \item \emph{Union:}
          $\mathrm{supp}(P(t_1) \cup P(t_2))
          = \mathrm{supp}(P(t_1)) \cup \mathrm{supp}(P(t_2))$.
    \item \emph{Projection/Selection:}
          $\mathrm{supp}(P(\pi(t))) \supseteq \mathrm{supp}(P(t))$
          and
          $\mathrm{supp}(P(\sigma(t))) \subseteq \mathrm{supp}(P(t))$.
  \end{enumerate}
\end{proposition}
\begin{proof}
  (a)~A join requires both premises to hold simultaneously.
  Under determination $D$, $t_1 \bowtie t_2$ is nonempty iff both
  $P_D(t_1) \neq 0$ and $P_D(t_2) \neq 0$ (semiring multiplication
  has no zero divisors in $\mathbb{N}[X]$).
  Hence $D \in \mathrm{supp}(P(t_1 \bowtie t_2))$ iff
  $D \in \mathrm{supp}(P(t_1)) \cap \mathrm{supp}(P(t_2))$.

  (b)~A union provides alternative derivation paths.
  $P_D(t_1 \cup t_2) = P_D(t_1) + P_D(t_2)$; this is nonzero iff at
  least one summand is nonzero.

  (c)~Projection can only add derivation paths (combining tuples that
  agree on projected attributes); selection can only remove them.
\end{proof}

\subsection{The Filtration in Detail}

The filtration $\mathcal{F}_0 \subseteq \mathcal{F}_1 \subseteq \cdots
\subseteq \mathcal{F}_d = 2^{\mathcal{D}}$ is defined in
Section~\ref{sec:filtration}.
Here we verify that it interacts correctly with the semiring operations
and with query evaluation.

\begin{proposition}[Filtration closure under semiring operations]
  For any $k$, if $S_1, S_2 \in \mathcal{F}_k$, then
  $S_1 \cup S_2 \in \mathcal{F}_k$ and
  $S_1 \cap S_2 \in \mathcal{F}_k$.
\end{proposition}
\begin{proof}
  A set is in $\mathcal{F}_k$ iff it is a union of level-$k$ equivalence
  classes.
  The union of two such sets is again a union of level-$k$ classes.
  For intersection: if $D \in S_1 \cap S_2$ and $D'$ agrees with $D$ at
  level $k$, then $D' \in S_1$ (since $S_1 \in \mathcal{F}_k$) and
  $D' \in S_2$ (since $S_2 \in \mathcal{F}_k$), so
  $D' \in S_1 \cap S_2$.
\end{proof}

\begin{corollary}[Query evaluation preserves filtration level]
  If $\mathrm{supp}(P(t_1)) \in \mathcal{F}_k$ and
  $\mathrm{supp}(P(t_2)) \in \mathcal{F}_k$, then
  $\mathrm{supp}(P(t_1 \bowtie t_2)) \in \mathcal{F}_k$ and
  $\mathrm{supp}(P(t_1 \cup t_2)) \in \mathcal{F}_k$.
  Query evaluation cannot increase query-relative depth beyond the
  maximum depth of its inputs.
\end{corollary}

% ===============================================================
\section{Transactional Worked Determination Provenance}
\label{sec:appendix-concurrency}
% ===============================================================

This appendix instantiates determination provenance for the transactional sandbox
used in Section~\ref{sec:transactions}.
We make explicit the commitments,
their dependency structure, and the resulting provenance annotations.
The goal is to justify the determination shapes and depths summarized in
Figure~\ref{fig:det-tables}.

Throughout, commitments rule out admissible executions and therefore
constitute genuine semantic commitments.
Once a resolving determination is fixed, the specification becomes single-valued
and provenance reduces to ordinary derivational provenance.

% ===============================================================
\subsection{Serializability}
% ===============================================================

\paragraph{Determination structure.}
Under conflict serializability, admissible outcomes are those whose conflict
graph is acyclic.
At runtime, the system's commit and abort decisions resolve ambiguity.
When the system also controls scheduling (Section~\ref{sec:scheduling-basis}),
ordering commitments $\varphi_{T_i \prec T_j}$ record which interleaving was
chosen; otherwise, the interleaving is an environment fact and only
commit/abort decisions are commitments.
This appendix uses the enriched (scheduling) basis for expository clarity.

For the running example below, the conflict graph is acyclic---no aborts were
needed---so the determination consists entirely of ordering commitments
(scheduling decisions that determined the conflict edges).
\paragraph{Data, query, and history.}
Let $S(k,y)$ be a relation and consider the query
$Q(y) \drule S(k,y)$.
The database is initially empty.
Five transactions execute:
\[
  \begin{array}{ll}
    T_1: \textsf{insert } S(1,b) \ (x_1) \qquad &
    T_2: \textsf{insert } S(2,b) \ (x_2)          \\
    T_3: \textsf{insert } S(3,c) \ (x_3) \qquad &
    T_4: \textsf{insert } S(4,d) \ (x_4)          \\
    T_5: \textsf{delete } S(4,d).               &
  \end{array}
\]
A query transaction $T_Q$ evaluates $Q$.

The history alone does not determine whether $d$ is visible to $T_Q$; this depends
on the relative ordering of $T_4$, $T_5$, and $T_Q$.

\paragraph{Resolving determinations.}
Two valid resolving determinations under serializability are:
\[
  D_{\mathsf{in}} \triangleq
  T_1 \prec T_2 \prec T_3 \prec T_4 \prec T_Q \prec T_5,
  \qquad
  D_{\mathsf{out}} \triangleq
  T_1 \prec T_2 \prec T_3 \prec T_4 \prec T_5 \prec T_Q.
\]
Both are conjunctions of ordering predicates and therefore arise in a single
semantic stage.

\paragraph{Provenance.}
Once a resolving determination is fixed, provenance is purely derivational.
Conditioned on $D_{\mathsf{in}}$, the resolved snapshot contains $b,c,d$:
\[
  P_{D_{\mathsf{in}}}(b)=x_1+x_2,\quad
  P_{D_{\mathsf{in}}}(c)=x_3,\quad
  P_{D_{\mathsf{in}}}(d)=x_4.
\]
Conditioned on $D_{\mathsf{out}}$, tuple $d$ is absent and classical why-not
provenance applies.

Determination provenance records the semantic assumption under which the
derivation holds; e.g.,
\[
  P_{D_{\mathsf{in}}}(d) = x_4.
\]

\paragraph{Same observation, different derivational provenance.}
Even when multiple serializations yield the same observation, they need not agree
on derivational provenance.
Adding
$T_{-2}:\ \textsf{delete } S(2,b)$
yields executions in which $b$ is observed with provenance $x_1$ or $x_1+x_2$,
depending on the serialization.
Thus determination provenance distinguishes semantic explanations even when the
observable outcome is unchanged.

% ===============================================================
\subsection{Snapshot Isolation}
\label{sec:appendix-SI}
% ===============================================================

\paragraph{Determination structure.}
As discussed in Section~\ref{sec:transactions}, all isolation levels share the
same basic enforcement pattern (orient conflict edges, abort to break forbidden
cycles), but differ in their commitment bases.
Snapshot isolation (SI) is multiversion: each transaction reads from a specific
committed state, so the determination must additionally record \emph{which
snapshot each transaction observed} and \emph{which concurrent writer wins}
on each contested object.

The SI commitment basis includes two kinds of commitments beyond
serializability's commit/abort:
\begin{itemize}[nosep]
  \item \emph{Snapshot predicates} $\varphi_{\snap(T)=U}$: transaction $T$
        reads from a snapshot containing exactly the effects of
        transactions in $U$.
  \item \emph{First-committer-wins (FCW) predicates}
        $\varphi_{\mathrm{fcw}(x,T_i)}$: on object $x$, transaction $T_i$
        is the first committer among concurrent writers---all other
        concurrent writers on $x$ must abort.
\end{itemize}
Non-commutativity arises from FCW-to-snapshot dependencies: an FCW
decision on object $x$ determines which version of $x$ is visible in
subsequent snapshots.
Snapshot selections commute with each other (they are independent
observations); FCW decisions on distinct objects likewise commute.
But a snapshot selection that depends on an FCW outcome cannot commute
with that FCW decision.
Depth arises from chains of such dependencies: an FCW decision enables a
snapshot, which enables a write, which triggers another FCW decision.

\paragraph{Extended history.}
To illustrate FCW-induced depth, add two transactions that write the
\emph{same} object concurrently.
Assume the database initially contains $S(5,e)$, and two transactions
execute concurrently:
\[
  \begin{array}{ll}
    T_6: \textsf{write } S(5,e') \qquad &
    T_7: \textsf{write } S(5,e'').
  \end{array}
\]
A query transaction $T_Q$ reads $S(5,\_)$ after both have committed.
Under SI, only one of $T_6$ or $T_7$ can commit (FCW on object $S(5,\_)$);
the other must abort.

\paragraph{Resolving determination.}
One admissible SI determination is:
\[
  \begin{aligned}
    D_{\mathrm{SI}} \triangleq {} &
    \{\snap(T_6){=}\emptyset,\; \snap(T_7){=}\emptyset\} \\
                                  & \seq\; \varphi_{\mathrm{fcw}(S(5),\,T_6)} \\
                                  & \seq\; \snap(T_Q){=}\{T_1,\dots,T_6\}.
  \end{aligned}
\]
Layer~1 assigns initial snapshots (commute).
Layer~2 resolves the write conflict: $T_6$ wins on $S(5)$, so $T_7$ aborts.
Layer~3 assigns $T_Q$'s snapshot, which depends on knowing that $T_6$
committed and $T_7$ did not.

\paragraph{Outcome and provenance.}
Under $D_{\mathrm{SI}}$, $T_Q$ reads $S(5,e')$ (written by $T_6$).
The derivational provenance is $P_{D_{\mathrm{SI}}}(e') = x_6$.
Under the alternative determination with
$\varphi_{\mathrm{fcw}(S(5),T_7)}$, $T_Q$ would read $S(5,e'')$
instead---a contingent outcome.

\paragraph{Write-skew and serializability.}
To additionally illustrate write-skew (an SI anomaly that requires no FCW),
consider the classic pattern: $T_A$ reads $x$, writes $y$; $T_B$ reads $y$,
writes $x$.
Under SI with snapshots preceding both commits, both transactions commit
(no write-write conflict, so FCW does not apply).
The resulting rw-rw cycle is not serializable.
Determination provenance records the snapshot assignments under which this
anomaly arises; restricting to SI determinations whose snapshots admit a
topological sort recovers exactly the serializable outcomes.

\paragraph{Determination depth.}
Unlike serializability, which resolves ambiguity in a single commuting stage,
snapshot isolation interleaves FCW decisions with snapshot selections that
depend on them.
Worst-case depth is $O(n)$ (chains of FCW $\to$ snapshot dependencies),
as recorded in Figure~\ref{fig:det-tables}.
Once an SI determination is fixed, provenance again reduces to depth--0
derivational provenance.

% ===============================================================
\subsection{Certificates and Semantic-Change Queries}
% ===============================================================

A \emph{certificate} over-approximates a commitment's effect: it
summarizes which outcomes a commitment excludes, admitting possibly more
but never fewer (formal definition in
Appendix~\ref{app:systems-lineage}).
Under serializability, a certificate need only record ordering predicates
sufficient to justify the observation.
Under snapshot isolation, answering semantic-change queries---such as
whether an SI outcome admits a serial explanation---requires preserving
the snapshot structure itself.
Certificates are query-parametric: richer
questions require retaining more semantic information
(Appendix~\ref{app:systems-lineage}).

% ===============================================================
\section{Protocol-Specific Determination Structures}
\label{app:protocols}
% ===============================================================

The body (Section~\ref{sec:transactions}) presents determination provenance
using a commit/abort basis where operation interleaving is environment and
the system's only irrevocable decisions are transaction commit and abort at
validation time.
This is essentially optimistic concurrency control
(OCC)~\cite{kung1981optimistic}: transactions execute speculatively, and
the semantic resolution occurs at validation.
The depth characterization of Section~\ref{sec:transactions}---depth~$1$
without cycles, $\ge 2$ with overlapping cycles---applies directly to OCC.

This appendix instantiates the framework under two other canonical
protocols---two-phase locking (2PL) and multiversion timestamp ordering
(MVTO)---to show how protocol mechanisms map to commitment structure and
to expose the resulting differences in determination depth.

\subsection{Two-Phase Locking (2PL)}

Under 2PL, a transaction acquires locks before accessing objects and
releases them only after its last access (the ``two phases'': growing and
shrinking).
The determination structure differs from the protocol-agnostic treatment
in two ways.

\paragraph{Lock acquisition as ordering commitment.}
Each lock acquisition implicitly fixes an ordering on the locked
object: if $T_i$ holds a write lock on $x$ and $T_j$ requests a
conflicting lock, the system has irrevocably decided $T_i \prec T_j$ on $x$.
Under the enriched scheduling basis
(Section~\ref{sec:scheduling-basis}), this is exactly the ordering
commitment $\varphi_{T_i \prec T_j}$.
The key difference from the protocol-agnostic view is \emph{timing}:
ordering commitments are made incrementally as locks are acquired
(distributed throughout execution), rather than concentrated at commit
time.

\paragraph{Deadlock as forced abort.}
A deadlock cycle $T_1 \to T_2 \to \cdots \to T_m \to T_1$ in the
wait-for graph means the lock-acquisition commitments already made are
jointly inconsistent with all transactions committing.
The system resolves this by aborting one transaction (the ``victim''),
which is an abort commitment $\varphi_{\mathsf{abort}(T_v)}$.
The choice of victim is itself a commitment: different victims yield
different determinations.

\paragraph{Determination structure.}
Under strict 2PL (locks held until commit), the ordering commitments
made during the growing phase commute on distinct objects
(Proposition~\ref{prop:commit-commute}).
Non-commutativity arises only when deadlock forces a victim choice that
affects subsequent lock availability.
Hence:
depth~$1$ when no deadlocks occur (all lock-acquisition commitments
commute);
depth~$\ge 2$ when deadlock resolution enables or blocks subsequent
lock acquisitions.
This matches the body's characterization
(Proposition~\ref{prop:ser-depth}): overlapping cycles in the conflict
graph correspond to deadlock cycles in the wait-for graph.

\paragraph{Provenance implications.}
Under 2PL, the determination records not just \emph{which} transactions
committed but \emph{when} each ordering was fixed (at lock acquisition
time).
Two executions that produce the same serial order but acquire locks in
different sequences yield the same resolved outcome but different
determination provenance---the commitments were made at different points
in the history.
This finer-grained structure is invisible in the protocol-agnostic
treatment but may matter for diagnostic queries about lock contention.

\subsection{Multiversion Timestamp Ordering (MVTO)}

Under MVTO~\cite{reed1978naming}, each transaction $T_i$ receives a
unique timestamp $ts_i$ at start.
Reads and writes are governed by the timestamp order: $T_i$ reads the
latest version of $x$ written by a transaction with timestamp
$\le ts_i$, and $T_i$'s write of $x$ creates a new version tagged
$ts_i$.
A write by $T_i$ is rejected (and $T_i$ aborted) if some transaction
$T_j$ with $ts_j > ts_i$ has already read the previous version of $x$
--- the ordering $T_i \prec T_j$ is already committed, and $T_i$'s
write would retroactively invalidate $T_j$'s read.

\paragraph{Timestamp assignment as semantic commitment.}
The primary semantic commitment in MVTO is the timestamp assignment itself.
Assigning $ts_i < ts_j$ irrevocably fixes $T_i \prec T_j$ in the serial
order on every object they both access.
This is an ordering commitment
$\varphi_{T_i \prec T_j}$ in the enriched scheduling basis
(Section~\ref{sec:scheduling-basis}), but with a crucial structural
property: a single timestamp assignment simultaneously fixes the
ordering of $T_i$ relative to \emph{all} other active transactions.
Version reads are not separate commitments---they are consequences of
the timestamp order.

\paragraph{Forced aborts.}
Unlike 2PL, MVTO never blocks: conflicting operations are either
served from an appropriate version (reads) or rejected immediately
(writes that violate timestamp order).
Rejection forces an abort commitment
$\varphi_{\mathsf{abort}(T_i)}$, but this is not a free choice---it
is determined by the timestamp assignments already made.
MVTO is therefore deadlock-free.

\paragraph{Determination structure.}
Because timestamp assignments on non-conflicting transactions commute
(the relative order is irrelevant if they access disjoint objects),
and because aborts are forced (not chosen), the determination under
MVTO has a particularly simple structure:
\begin{itemize}[nosep]
  \item All timestamp assignments commute on non-conflicting pairs,
        forming a single layer of width equal to the number of
        conflicting pairs.
  \item Forced aborts are entailed by the timestamp layer---they add
        no independent commitments and no additional depth.
\end{itemize}
Hence MVTO determinations have depth~$1$ in all cases: the timestamp
layer resolves all ambiguity in a single commuting stage.
This contrasts with 2PL, which may require depth~$\ge 2$ under
deadlock.

\paragraph{Comparison across protocols.}
All three protocols implement serializability but differ in
determination structure.
OCC (the body's default) defers all semantic decisions to validation time:
depth~$1$ without cycles, $\ge 2$ with overlapping cycles.
2PL makes ordering commitments incrementally at lock acquisition:
same depth characterization, but deadlock introduces forced aborts
whose resolution can add layers.
MVTO fixes the full ordering in a single timestamp assignment:
depth~$1$ unconditionally, at the cost of more aborts (fewer valid
resolving determinations for a given workload).
The three protocols thus trace a spectrum from late resolution
(OCC) through incremental resolution (2PL) to eager resolution
(MVTO), with determination depth decreasing along this axis.

\section[Datalog with Negation: Worked Determination Provenance]{Datalog$^{\neg}$ Worked Determination Provenance}
\label{sec:appendix-datalog}

This appendix instantiates determination provenance for the Datalog-with-negation
sandbox.
Following Section~\ref{sec:negation-preview}, we begin with the most resolved
semantics---stable models---and then show how coarser semantics are recovered
by reading less of the determination semiring.
We work through the running example below, making explicit the commitments,
their dependency structure, and the resulting provenance annotations.

\begin{example}[Datalog with negation]
  \label{ex:datalog-negation}
  Consider the following program $P$ over EDB facts in a history $H$.
  For notational economy we use a fixed constant $c$ and take all predicates unary, so the EDB fact is $a(c)$ and the derived atoms are $p(c),q(c),r(c),s(c)$.
  \begin{align*}
    p(c) & \leftarrow a(c).      & r(c) & \leftarrow \neg s(c). \\[1mm]
    q(c) & \leftarrow \neg p(c). & s(c) & \leftarrow \neg r(c).
  \end{align*}
  Assume $H$ contains the single extensional fact $a(c)$.
  The pair $(P,H)$ contains both a stratified fragment
  ($q(c)$ depends negatively on $p(c)$, and $p(c)$ depends positively on $a(c)$)
  and a cycle through negation ($r \leftrightarrow \neg s$).
  We use this single instance to exhibit three distinct readings of the
  determination semiring.
\end{example}

Throughout, we use \emph{sealing predicates}
$\varphi_{\seal}(X)$, which commit that the set of atoms $X$ is complete:
no additional atoms in $X$ will be derived in any admissible outcome.
Such predicates rule out outcomes and therefore constitute genuine semantic
commitments.

\subsection{Stable Model Semantics}

\paragraph{Determination structure.}
Stable semantics insists on total two-valued models.
The resolving determinations share a stratified prefix---sealing each
stratum before negation in the next is evaluated---and then branch to
resolve the cycle.

\paragraph{Resolving determinations.}
For the running example, resolution begins with the stratified-prefix sealing
\[
  \varphi_{\seal}(S_1)
  \seq \cdots \seq
  \varphi_{\seal}(S_k),
\]
where each $S_i$ is the set of atoms whose truth is fixed at stratum~$i$.
These sealing predicates do \emph{not} commute across strata: sealing $S_i$
is a prerequisite for applying negation commitments in $S_{i+1}$.
After this prefix, the negation cycle admits two incompatible resolutions:
\[
  D^{(r)}
  \;\triangleq\;
  \bigl(
  \varphi_{\seal}(S_1)
  \seq \cdots \seq
  \varphi_{\seal}(S_k)
  \bigr)
  \seq
  \varphi_{r(c)=\mathbf{t}} \cdot \varphi_{s(c)=\mathbf{f}},
\]
\[
  D^{(s)}
  \;\triangleq\;
  \bigl(
  \varphi_{\seal}(S_1)
  \seq \cdots \seq
  \varphi_{\seal}(S_k)
  \bigr)
  \seq
  \varphi_{s(c)=\mathbf{t}} \cdot \varphi_{r(c)=\mathbf{f}}.
\]
Within either determination, the cycle-resolution commitments are logically
simultaneous and commute.
The determination height is $k{+}1$: the $k$ stratification stages plus one
additional non-commuting branching stage.

\paragraph{Provenance.}
Determination provenance records which stable determination is assumed:
\[
  P_{D^{(r)}}(r(c)) = 1,
  \qquad
  P_{D^{(s)}}(s(c)) = 1.
\]
Only after this choice does ordinary derivational provenance apply.
For atoms determined by the stratified prefix:
$P_{D^{(r)}}(p(c)) = P_{D^{(s)}}(p(c)) = P(a(c))$.

\subsection{Relaxing to Well-Founded Semantics}

\paragraph{Determination structure.}
Well-founded semantics (WFS) makes the same $k$ stratified commitments as
stable semantics but does not discharge the layer that resolves the cycle.
It then performs a nontrivial computation---the alternating
fixpoint~\cite{vanGelder1991wellFounded}---to classify which atoms are
forced true or false by the existing commitments and which remain genuinely
ambiguous.

The alternating fixpoint operates on the knowledge ordering
$\mathbf{u} \le_k \mathbf{t}$ and $\mathbf{u} \le_k \mathbf{f}$
(with $\mathbf{t}$ and $\mathbf{f}$ incomparable), moving atoms upward
from ``unknown'' to ``known true'' or ``known false'' by alternating
between deriving positive consequences and eliminating unfounded sets.
This ordering is an instance of the outcome order $\Ord$.
The distinction between the alternating fixpoint and a commitment is
precisely the distinction between these two operations:
\begin{itemize}[nosep,leftmargin=*]
  \item A \emph{commitment} operates on $2^O$: it replaces the admissible
        set with a proper subset, excluding outcomes.
  \item The \emph{alternating fixpoint} operates on $O$ via $\Ord$: it
        replaces the observed outcome $o$ with some $o'$ where
        $o \Ord o'$---an upward move that the specification recognizes
        as adding detail, not contradicting what came before.
        It is the specification's choice of $\Ord$ that makes the
        alternating fixpoint an entailment rather than a commitment.
\end{itemize}

The correspondence to the determination semiring is exact (for programs
with stable models): WFS assigns $\mathbf{t}$ to an atom iff it holds in
every stable model (robust); $\mathbf{f}$ iff it fails in every stable
model (robustly absent); and $\mathbf{u}$ iff it holds in some but not all
(contingent).

\paragraph{Determination.}
The WFS determination for the running example is the same stratified prefix:
\[
  D_{\mathrm{wfs}}
  \;\triangleq\;
  \varphi_{\seal}(S_1)
  \seq
  \varphi_{\seal}(S_2)
  \seq \cdots \seq
  \varphi_{\seal}(S_k).
\]
This is \emph{not} a resolving determination in the sense of
Definition~\ref{def:determined}: the specification $\Spec \mid D_{\mathrm{wfs}}$
is not a function, because multiple two-valued completions of the cycle
remain admissible.
WFS accepts this and classifies the residual ambiguity.

For the running example, the stratified prefix determines:
$p(c)=\mathbf{t}$ (robust), $q(c)=\mathbf{f}$ (robustly absent),
while the $\neg$-cycle atoms have partial support:
$r(c)$ and $s(c)$ each hold in exactly one of the two stable models.
WFS reports this as $r(c)=\mathbf{u}$, $s(c)=\mathbf{u}$.

\paragraph{A nontrivial example.}
The alternating fixpoint can discover robust atoms that depend on the cycle
without resolving it.
Consider adding the rule $d(c) \leftarrow r(c);\; d(c) \leftarrow s(c)$.
Then $d(c)$ holds in every stable model (since exactly one of $r(c)$, $s(c)$
is true in each), so $\mathrm{supp}(P(d(c))) = \mathcal{D}$ and WFS
correctly assigns $d(c) = \mathbf{t}$.
The alternating fixpoint discovers this in multiple rounds---deriving $d(c)$
optimistically, confirming it survives skeptical revision---without making
any commitment about which of $r(c)$ or $s(c)$ is true.

\paragraph{Provenance.}
For atoms determined by the stratified prefix, provenance is classical:
$P_{D_{\mathrm{wfs}}}(p(c)) = P(a(c))$.
For $\neg$-cycle atoms, determination provenance has partial support:
$P(r(c)) = \{(D^{(r)}, 1)\}$, $P(s(c)) = \{(D^{(s)}, 1)\}$.
WFS does not select a determination, so it reports these as $\mathbf{u}$
rather than reading off the conditioned provenance.
But the determination semiring retains the \emph{contingency structure}
of these atoms: the support $\{D^{(r)}\}$ records exactly which
determinations make $r(c)$ hold and which do not.
This is not provenance---provenance requires resolution
(Theorem~\ref{thm:resolution-implies-monotone})---but a property
\emph{of} the determination provenance function, read at filtration
level~$k$ without selecting a determination.

\paragraph{Relationship to the filtration.}
The filtration of the stable-model determination semiring
(Section~\ref{sec:filtration}) \emph{encodes} the classification that
WFS \emph{computes}.
Robust, robustly absent, and contingent support in the semiring correspond
to WFS's $\mathbf{t}$, $\mathbf{f}$, and $\mathbf{u}$ respectively.
The filtration is a static algebraic structure; the alternating fixpoint
is an algorithm that discovers, for each atom, exactly the support
classification that the filtration carries.

\subsection{Relaxing Further to Stratified Semantics}

\paragraph{Determination structure.}
Stratified semantics applies only to programs (or fragments of programs)
whose dependency graph admits a stratification---no cycles through negation.
For the running example, the stratified fragment consists of the predicates
$p(c)$ and $q(c)$; the cycle $(r, s)$ is outside its scope.

The resolving determination for the stratified fragment is the same
stratified prefix used by stable and well-founded semantics:
\[
  D_{\mathrm{strat}}
  \;\triangleq\;
  \varphi_{\seal}(S_1)
  \seq
  \varphi_{\seal}(S_2)
  \seq \cdots \seq
  \varphi_{\seal}(S_k).
\]
For stratifiable programs, this prefix is itself a resolving determination:
$|\Spec \mid D_{\mathrm{strat}}| = 1$.
For programs with cycles, it is not---the $\neg$-cycle atoms remain unresolved,
and stratified semantics is simply undefined for them.

\paragraph{Provenance.}
Once $D_{\mathrm{strat}}$ is fixed, derivational provenance applies for
the stratifiable fragment:
$P_{D_{\mathrm{strat}}}(p(c)) = P(a(c))$.
Absences such as $\neg q(c)$ are derivational consequences of the sealed
lower stratum and require no additional commitments.

\subsubsection{Preservation Across Negation Semantics}

Holding the program and EDB fixed, different negation semantics induce
different readings of the determination semiring, and preservation
(Section~\ref{sec:det-semiring}) can be checked directly.

For the running example, $p(c)$ has full support
($\mathrm{supp} = \mathcal{D}$): it holds under every stable model, is
robust under WFS, and is derived by stratified evaluation.
It is preserved across all three semantics.
By contrast, $r(c)$ has partial support ($\{D^{(r)}\}$): it holds under
one stable model, is $\mathbf{u}$ under WFS, and is undefined under
stratified semantics.
It is not preserved from stable to well-founded or stratified.

\subsection{Certificates and Semantic-Change Queries}

The certificate required to justify a provenance answer depends on the
query.
A minimal certificate for derivational provenance of $p(c)$ need only
preserve the sealing commitments for the stratified prefix.
Answering a preservation query (``Would $q(c)$ become true under stable
semantics?'') requires a strictly stronger certificate that preserves the
unresolved negation structure itself.
This query-parametric nature of certificate sufficiency is developed
further in Appendix~\ref{app:systems-lineage}.

% ================================================================
\section{Determination Provenance for Systems Lineage}
\label{app:systems-lineage}
\label{app:certificates}
% ================================================================

Systems lineage---as practiced in distributed tracing~\cite{sigelman2010dapper},
workflow provenance~\cite{seltzer2005provenance}, and pipeline
auditing~\cite{bates2015trustworthy}---records \emph{which} services,
operations, or data sources contributed to an observed result.
This is lineage in the sense of Cui et al.~\cite{cui2000lineage}:
set-valued input attribution without algebraic structure.
It answers ``which inputs contributed?'' but not
``how did they combine?'' (no semiring), ``would the result
survive a different execution?'' (no counterfactuals), or ``which
assumptions were necessary?'' (no determination).
The result is verbose (full execution traces are retained) yet
expressively limited (only positive, single-execution queries are
supported).

Determination provenance addresses both deficiencies.
This appendix maps the three levels of structure
(Section~\ref{sec:three-levels}) to concrete capabilities that the
theory provides for systems lineage, then develops two orthogonal
dimensions of parsimony that make the theory practical.

\subsection{Level 1: Repeatability from a Single Determination}

A resolving determination records the non-deterministic commitments
that resolved the specification to a function.
Once a determination is fixed, the remaining execution is deterministic:
given the history and the commitments, the resolved outcome can be
reconstructed by re-execution.
(If entailment steps involve non-determinism---e.g., hash-based
tie-breaking---the determination must be augmented with random seeds
for those steps; we assume deterministic entailment throughout.)

This is already a compression over full-trace lineage: rather than
recording every intermediate state, the system retains only the
commitment sequence.
All derivational detail can be reconstructed on demand.
Once reconstructed, classical semiring provenance applies, answering
positive provenance queries (why-provenance, how-provenance) with full
algebraic structure.

More broadly, determination provenance serves as a \emph{query layer}
on top of existing logs.
Full traces are retained as today; the determination (the
non-deterministic commitments and their structure) is extracted as a
\emph{semantic index} over the log.
Diagnostic queries---robustness, counterfactuals, root-cause
identification---are evaluated against the determination structure
($O(k)$ commitments) rather than by scanning raw events ($O(ne)$
log records).
The algebraic structure enables queries that log-scanning cannot answer
at all (e.g., ``under what fraction of admissible schedules does the
SLA hold?''), and answers those it can more efficiently by routing
through the semiring rather than correlating events.

\begin{example}[Distributed tracing {\`a} la Dapper~\cite{sigelman2010dapper}]
  \label{ex:dapper}
  A web-search request spawns a tree of spans: the frontend ($F$)
  calls a cache ($C$) and an ad server ($A$) in parallel; the cache
  misses and calls a database ($D$).
  Each span has client-send/server-receive events on different hosts
  with unsynchronized clocks.
  Causal constraints ($\mathsf{cs}_X \prec \mathsf{sr}_X$,
  $\mathsf{ss}_X \prec \mathsf{cr}_X$) are known, but the relative
  wall-clock durations of $C$ and $A$ are not---multiple orderings are
  admissible.

  A resolving determination commits to clock alignments
  ($\varphi_{\mathrm{align}(h_i,h_j)}$) between hosts, fixing a single
  execution timeline.
  Once fixed, critical-path analysis is deterministic: either $C$
  (cache miss + DB) or $A$ (ad auction) is on the critical path.

  Determination provenance enables queries that Dapper cannot answer:
  \begin{itemize}[nosep]
    \item \emph{Is span $D$ robustly on the critical path?}
          If $\mathrm{supp}(P(\mathrm{cp}=D)) = \mathcal{D}$, the
          database is the bottleneck under every admissible clock
          alignment.
    \item \emph{Under which clock assumptions does the ad server
          dominate latency?}
          The support $\mathrm{supp}(P(\mathrm{cp}=A))$ identifies
          exactly the determinations where $A$ is critical.
    \item \emph{Would migrating the cache to a closer datacenter
          change the critical path?}
          A preservation question: compare supports under the current
          spec vs.\ a spec with tighter latency bounds on $C$.
  \end{itemize}
\end{example}

\subsection{Level 2: Robustness, Tail Bounds, and Scheduler Design}

Supports across the space of resolving determinations enable queries
that systems lineage cannot express.

\paragraph{Robustness and fragility.}
The ratio $|\mathrm{supp}(P(t))| / |\mathcal{D}|$ measures how
\emph{fragile} an outcome is: a result with support ratio $0.99$ is
nearly robust (almost any schedule produces it); one with ratio $0.01$
depends on a very specific interleaving.
Boolean robustness ($\mathrm{supp} = \mathcal{D}$) is the extreme case.

\paragraph{Structural tail bounds.}
Consider a database with $n$ concurrent transactions and a latency
predicate $\ell(T_Q) < \tau$ (``query $T_Q$ completes within $\tau$
ms'').
The support of this predicate---the set of serializations under which
the SLA is met---gives a \emph{structural} tail-bound estimate:
$|\mathrm{supp}(\ell < \tau)| / |\mathcal{D}|$ is the fraction of
admissible schedules satisfying the SLA, without empirical sampling.
If this fraction is $0.95$, the system has a structural p95 guarantee.

\paragraph{Diagnosis.}
When the tail is violated, the \emph{complement} of the support
identifies the offending determinations.
If all violating determinations share a common ordering commitment
$\varphi_{T_i \prec T_j}$, that commitment is the root cause of the
tail---a specific conflict whose resolution determines whether the SLA
is met.

\paragraph{Scheduler design.}
A scheduler that avoids the offending commitment (e.g., never
serializes $T_i$ before $T_j$ on the contested object) eliminates the
tail without changing the isolation level or the workload.
This is a new optimization objective: not ``minimize average latency''
(a single-determination metric) but ``maximize the support of the SLA
predicate'' (a distributional metric over the determination semiring).
Baccaert et al.~\cite{baccaert2026makespan} optimize makespan within a
single determination; the determination semiring enables optimization
\emph{across} determinations.

\subsection{Level 3: Preservation Across Specifications}

Supports across variant specifications answer preservation questions:
does an outcome that holds under one specification also hold under
another?
Examples include isolation-level migration (does a transaction's result
survive a move from SI to
serializability?~\cite{vandevoort2025mixed}), semantic-change queries
(would a Datalog result survive a change from stable to well-founded
semantics?), and specification tightening (which results are preserved
when migrating to a stricter consistency model?).
These are across-specification questions that require comparing
determination semirings of different specs---a capability entirely
absent from systems lineage.

\subsection{Parsimony via Truncation (Filtration)}
\label{sec:parsimony-truncation}

The filtration (Section~\ref{sec:filtration}) provides a structural
answer to ``which layers do I need?''
If a query family $\mathcal{Q}$ depends only on tuples whose supports
lie in $\mathcal{F}_k$, then layers $k{+}1, \ldots, d$ are irrelevant:
the determination can be truncated at depth $k$ without affecting any
answer in $\mathcal{Q}$.

Query-relative depth (Definition~\ref{def:query-depth}) makes this
precise: a tuple with $\mathrm{qdepth}(t) = k$ is fully explained by
the first $k$ layers.
A system that retains only the first $k$ layers of each determination
can answer all queries whose inputs have depth $\le k$---and by
Corollary~\ref{cor:propagation-filtration}, positive relational algebra
cannot increase depth beyond its inputs.

This is \emph{vertical} parsimony: discard layers that the query family
does not need.

\subsection{Parsimony via Compression (Certificates)}
\label{sec:parsimony-compression}

Within a layer that \emph{is} retained, the full commitment may carry
more detail than the query requires.
A \emph{certificate} is a sound over-approximation of a commitment's
effect: it may admit outcomes that the actual commitment excludes, but
excludes none that the commitment admits.

\begin{definition}[Certificate]
  A certificate for commitment $\varphi$ is a predicate
  $C : O \to \{\mathsf{true}, \mathsf{false}\}$ such that for all $H$,
  $\Spec(H \cdot \varphi) \subseteq \{o \in \Spec(H) \mid C(o)\}$.
\end{definition}

\begin{definition}[Sufficient certificate]
  A certificate $C$ is \emph{sufficient for a query family
  $\mathcal{Q}$} if for every $q \in \mathcal{Q}$ and every history $H$,
  $q((\Spec \mid \varphi)(H)) = q((\Spec \mid C)(H))$---that is,
  replacing $\varphi$ with $C$ yields the same answer.
\end{definition}

\begin{theorem}[Query monotonicity]
  If $\mathcal{Q}_1 \subseteq \mathcal{Q}_2$, every certificate
  sufficient for $\mathcal{Q}_2$ is sufficient for $\mathcal{Q}_1$.
\end{theorem}

\noindent
This is \emph{horizontal} parsimony: within each retained layer,
coarsen the representation to the minimum fidelity the query family
demands.

\begin{example}[Certificates in practice]
  Under serializability, a certificate for derivational provenance need
  only record the ordering predicates relevant to the queried tuple's
  conflict neighborhood.
  Answering a preservation query (``would this result survive under a
  different isolation level?'') requires a strictly stronger
  certificate that preserves the full conflict-graph structure.
  In distributed tracing, a certificate for critical-path analysis
  retains only the clock-alignment and ordering commitments on the
  path itself---not the full trace.
\end{example}

\paragraph{The two dimensions compose.}
For a given query family: first truncate (filtration identifies the
deepest relevant layer), then compress each retained layer (certificates
identify the minimum per-layer fidelity).
Together they define a well-parameterized design space for parsimonious
systems lineage.
Fixing the parameters---choosing the query family, computing minimal
certificates, implementing adaptive retention---is the systems work
that remains.

% ================================================================
\section{Open Questions}
\label{app:open-questions}
% ================================================================

Several structural questions about determinations remain open.

\paragraph{Dynamic Foata normal form.}
In classical trace theory~\cite{mazurkiewicz1977concurrent}, the Foata normal
form canonically represents a word in a partially commutative monoid as a
sequence of maximal independent layers.
The layered structure of a determination is the analog---but commutation is
\emph{dynamic}: whether $\varphi$ and $\psi$ commute depends on the current
admissible set, which changes as earlier layers are discharged.
Characterizing when two determinations are equivalent (produce the same
resolved specification) would give determinations a precise algebraic
identity beyond their definition.

\paragraph{Depth under history extension.}
Under a persistent basis, the admissible set can only shrink as history grows.
\emph{Conjecture:} determination depth is monotonically non-increasing under
history extension---as more information arrives, resolution becomes easier
(or at least no harder).

\paragraph{Query-relative depth.}
A provenance query may depend on only a subset of the determination's layers.
Characterizing which queries require which layers would provide a formal
basis for adaptive log retention, connecting to layer certificates
(Appendix~\ref{app:certificates}).

\paragraph{Reversible commitments.}
In settings with rollback (transactional abort-and-retry, speculative
execution, backtracking search), commitments can be undone.
Extending the framework to reversible commitments---replacing monoids with
groups at each layer---is open.

\bibliographystyle{ACM-Reference-Format}
\bibliography{references}

\end{document}
